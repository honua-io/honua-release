# Lambda GA bill — evidence ledger (release#282, ruling A)

State of the evidence as of **2026-09-07 03:35Z**, the start of the last run
recorded here; §6 shows how to read anything the lane has run since. Nothing in
this document is a pass. **No passing Lambda certification receipt exists**:
twelve live runs of the certification lane have completed, all twelve recorded
`result: fail` and `serving.result: noProof`, and Lambda therefore remains
`status: ga-target` / `qualification: pending` in
[`platform-manifest.yaml`](../platform-manifest.yaml).

**Why this file exists.** The [operator ruling A of 2026-09-06](https://github.com/honua-io/honua-release/issues/282)
promoted AWS Lambda from Preview to a 2026.1 **GA target** carrying the whole ECS
bill (live deploy, serving smoke, upgrade, rollback, destroy) on the real
serverless substrate, and the amendment is recorded in the
[quality contract](https://github.com/honua-io/honua-flow/blob/trunk/docs/2026.1-quality-contract.md).
The bill's five items are already enforced from three places — the
[cut checklist](RELEASE-CUT-CHECKLIST.md), the generated
[decision record](2026.1-release-decision-record.md), and the
[serverless operating envelope](2026.1-operating-envelope.md#5-aws-lambda-serverless-supported-target-and-limits)
with the manifest qualification gate behind it. What none of those carry is the
*current position*: which live evidence exists today, what it says, and what is
still missing. That is what this ledger records, and only that. It changes no
gate, grants no qualification, and cannot be cited as a receipt.

**What discharges the bill.** A passing certification receipt, produced by the
lane below, meeting the acceptance rules already written into the
[cut checklist](RELEASE-CUT-CHECKLIST.md) rows for
[release#282](https://github.com/honua-io/honua-release/issues/282). Only then do
the `supported` / `passed` / `qualificationReceipt` values described in the
[operating envelope](2026.1-operating-envelope.md#5-aws-lambda-serverless-supported-target-and-limits)
become declarable, and only then does release#282 close.

## 1. Bill status

| # | Bill item | Phase | Evidence owner | Status today |
| --- | --- | --- | --- | --- |
| 1 | Exact trunk Lambda x86_64 digest deploy, invoke, verify, teardown; true ECR digest | pre-cut | honua-server lane, honua-iac substrate | **PENDING.** The lane exists and now runs live; the ECR mirror step passes byte-exact (see §3, runs 8–12). No run has reached a passing receipt, and every run so far was dispatched against a later honua-server trunk nightly than the manifest's pinned Lambda digest (§3), so item 1 also needs a run bound to that pin — or a re-pin to the certified artifact. |
| 2 | Serving smoke, not a health probe: asserted fixture row count, staging write round-trip, authorization denial, cold-start duration | pre-cut | honua-server lane, honua-devops write target | **PENDING, implemented but unproven.** The assertions are written into the lane (§2) and its write target is a dedicated cert Function URL, never the customer-facing demo. No live run has executed them to a pass; every receipt to date reads `serving.result: noProof`. |
| 3 | Alias-shift upgrade and rollback of the exact candidate through `AwsLambdaGitOpsDeployBackend`, candidate-bound receipt, teardown of lane-created resources | candidate | honua-server lane | **PENDING (candidate-bound).** The lane drives the compiled production backend rather than a raw CLI alias flip, but the proof must be produced against the exact candidate, which does not exist yet. Pre-cut trunk evidence cannot substitute. |
| 4 | Re-pin `awsLambdaEcrDigest` from the real ECR mirror; re-read `contractVersions` from the booted candidate | candidate | release engineer + release operator | **PENDING (candidate-bound).** The manifest still holds the `pending-ecr-mirror` sentinel, which is the correct value until the candidate is mirrored. Procedure: [cut checklist](RELEASE-CUT-CHECKLIST.md) ECR mirror bootstrap row. |
| 5 | Serverless operating limits published; support matrix, docs, CLI and contract rows moved to Supported | pre-cut (limits, scope, aligned rows) + candidate (Supported flip) | honua-release (docs/data) + owning runtime/CLI packets | **PART LANDED.** The limits and the GA-target scope shipped in [release#290](https://github.com/honua-io/honua-release/pull/290) — [envelope §5](2026.1-operating-envelope.md#5-aws-lambda-serverless-supported-target-and-limits) — together with the fail-closed qualification gate. The **Supported** flip is deliberately withheld: it is gated on items 1–4, and `tools/validate_platform.py` rejects `supported` without a passed, candidate-bound receipt. That withheld half is candidate-bound and completes last, after items 3 and 4 (§5). |

Item 5 spans both phases and must not be read as one pre-cut gate. Its
**pre-cut half** — published serverless limits and GA-target scope, with the
matrix, docs, CLI and contract rows aligned at `ga-target` / `pending` — blocks
the **candidate cut** alongside items 1 and 2, and has landed. Its **remaining
half** — flipping those rows to `supported` / `passed` behind a receipt
reference — is candidate-bound, blocks the **GA cut/promotion** alongside items
3 and 4, and cannot precede them. That is the split the
[cut checklist](RELEASE-CUT-CHECKLIST.md) already carries as two separate rows:
pre-cut scope/qualification alignment, and the post-bill `supported`
declaration. Missing, skipped, stale or wrong-candidate evidence fails closed in
every case.

## 2. The certification lane and its substrate

| Piece | Identity | State |
| --- | --- | --- |
| Workflow | honua-server `.github/workflows/lambda-preview-certification.yml` (`workflow_dispatch`, serialized against `real-aws-certification` because both use the standing certification alias) | live, dispatched twelve times |
| Driver | honua-server `scripts/cloud/certify-lambda-preview.sh` with `scripts/cloud/lambda-certification.py`; contract documented in `scripts/cloud/lambda-certification.md`; offline self-test `scripts/cloud/test-certify-lambda-preview.py` | live |
| Deploy backend under test | the repository's actual `AwsLambdaGitOpsDeployBackend` / `AwsLambdaAliasClient`, compiled by the lane driver — not a raw CLI alias update | live |
| Substrate | honua-iac [#173](https://github.com/honua-io/honua-iac/pull/173) (merged 2026-09-06) plus [#175](https://github.com/honua-io/honua-iac/pull/175) — **applied to the certification account but not yet merged**; it carries the ECR repository-policy fix, the secrets-read grant, VPC/ENI and VPC-describe grants, the standing-alias Function URL, mirror-tag replacement, standing-function startup migrations and policy-compliant admin-password validation | applied, PR open |
| Lane inputs | honua-server repository variables `REALAWS_CERT_LAMBDA_PREVIEW_REPOSITORY`, `REALAWS_CERT_LAMBDA_PREVIEW_EXECUTION_ROLE_ARN`, `REALAWS_CERT_LAMBDA_FUNCTION`, `REALAWS_CERT_LAMBDA_ALIAS`, `REALAWS_CERT_LAMBDA_WRITE_BASE_URL`, `HONUA_DEMO_BASE_URL`; cert-environment secrets for the admin and deliberately under-scoped denial principals | configured (names only; no values are recorded anywhere in this repository) |

Two blockers recorded earlier in the [evidence map](2026.1-evidence-map.md)
(row **I-09**, audited 2026-09-05) are resolved and should not be re-filed: the
lane's two required repository variables are no longer absent, and the
arm64-only constraint is gone — the workflow takes an `architecture` dispatch
input, asserts it against the manifest's `awsLambdaArchitecture`, and resolves
the matching single-platform child of a multi-platform index. Everything else in
that row still stands, including the finding that the run must prove serving
rather than liveness.

The lane's own fail-closed posture is the reason this ledger can be short: a
failed or absent serving check writes `serving.result: noProof` and exits
nonzero, and the receipt carries fingerprints only — no endpoint URLs,
connection strings, keys or raw AWS logs.

## 3. Live runs to date

Twelve runs, 2026-09-06 18:35Z through 2026-09-07 03:35Z. All twelve were
dispatched with the same x86_64 source inputs, mirrored from GHCR into the
certification account's ECR repository:

| Dispatch input | Value |
| --- | --- |
| `source_image` | `ghcr.io/honua-io/honua-server:nightly-lambda-aot-3c4a8cd-amd64` |
| `source_digest` | `sha256:0b526ccb871b9a5cbd82312d5736a5bfccd1a21112e628e5c2ca5d26726f744c` |
| `server_revision` | `3c4a8cdad0f159ab4771b1bf166803aa661087e4` (honua-server trunk, 2026-09-06) |
| `architecture` | `x86_64` |

**That input is not the manifest pin.**
[`platform-manifest.yaml`](../platform-manifest.yaml) pins
`nightly-lambda-aot-4ca8326-amd64` /
`sha256:fad75a7643475f4fe41a0fd627e046104c49d8b6ca19fff026bc0171ab49b389`
(honua-server `4ca8326f`, 2026-08-30); the lane was driven from current trunk
instead while its owner iterated on the lane itself. That provenance is
ordinary, but it is a certification fact rather than a detail: bill item 1
requires the exact pinned digest, so **none of the runs below could have
discharged item 1 as the manifest stands, green or not.** Closing item 1 needs
either a run dispatched with the pinned digest, or a manifest re-pin to the
certified artifact and a run against that new pin (§4, blocker 5).

Every receipt artifact reads
`{"schema":"honua.lambda-preview-certification/v1","result":"fail","serving":{"result":"noProof"}}`.
"Lane source" is the honua-server head SHA the *workflow* ran from; it tracks
the lane fixes listed below and is independent of the artifact under test.

| # | Run | Started (UTC) | Lane source | Where it stopped |
| --- | --- | --- | --- | --- |
| 1 | [34052215320](https://github.com/honua-io/honua-server/actions/runs/34052215320) (attempt 2) | 2026-09-06 18:35 | `959a830b` | prepare: `get-function-url-config` failed, no Function URL on the alias; `serving noProof` |
| 2 | [34053195554](https://github.com/honua-io/honua-server/actions/runs/34053195554) | 18:54 | `959a830b` | same Function URL failure |
| 3 | [34053601724](https://github.com/honua-io/honua-server/actions/runs/34053601724) | 19:02 | `959a830b` | `Standing function skips migrations: noProof` — the standing cert function was configured to skip migrations |
| 4 | [34054001535](https://github.com/honua-io/honua-server/actions/runs/34054001535) | 19:09 | `959a830b` | exit 4: `ECR mirror config digest does not match the exact source artifact` (a re-encoding mirror, correctly rejected) |
| 5 | [34064826386](https://github.com/honua-io/honua-server/actions/runs/34064826386) | 22:43 | `1cd38cd0` | ECR manifest push refused: `TAG_INVALID` — the mirror tag written by run 4's rejected copy already existed and the repository is tag-immutable |
| 6 | [34065278695](https://github.com/honua-io/honua-server/actions/runs/34065278695) | 22:52 | `1cd38cd0` | exit 5: `Lambda create failed (configuration diagnostics suppressed)` |
| 7 | [34068427904](https://github.com/honua-io/honua-server/actions/runs/34068427904) | 2026-09-07 00:00 | `f0ceeff3` | exit 5: malformed `create-function` invocation |
| 8 | [34069684026](https://github.com/honua-io/honua-server/actions/runs/34069684026) | 00:23 | `22f6303a` | exit 5: `CreateFunction` denied by EC2 — the VPC/ENI grant was still missing |
| 9 | [34070228945](https://github.com/honua-io/honua-server/actions/runs/34070228945) | 00:34 | `22f6303a` | exit 11: an AWS CLI subcommand that does not exist in the runner's CLI version |
| 10 | [34078979087](https://github.com/honua-io/honua-server/actions/runs/34078979087) | 03:14 | `11c29212` | exit 6: the function booted and exited with a runtime error (`INIT_REPORT … Phase: invoke, Status: error`, init 531 ms) |
| 11 | [34079620255](https://github.com/honua-io/honua-server/actions/runs/34079620255) | 03:25 | `4c69e7f0` | exit 6: same shape, init 21 364 ms — the initialization reached the database/secrets path before failing |
| 12 | [34080138481](https://github.com/honua-io/honua-server/actions/runs/34080138481) | 03:35 | `6c6ae7bd` | exit 14: `first invoke REPORT has no positive cold-start Init Duration` — reached and passed the ECR mirror and function boot, failed the cold-start parse |

**Run 12 is the furthest the lane has reached, and it is still a fail.** The
lane owner reports in [honua-server#4490](https://github.com/honua-io/honua-server/pull/4490)
that this run is the first in which the ephemeral function answered the health
probe with 200 and a healthy body, after the substrate gained the secrets-read
grant and a policy-compliant admin password. **That statement is the lane
owner's, not a receipt**: run 12's own receipt records `result: fail` and
`serving.result: noProof`, the serving assertions of bill item 2 did not
execute, and this repository records no serving pass.

Cause of the run-12 failure, per the same PR: server initialization takes about
21 s on this substrate (Secrets Manager resolution plus the database connection
over the VPC), which exceeds Lambda's init window, so the runtime re-runs init
inside the first invoke and the only `Init Duration` appears on an
`INIT_REPORT … Phase: invoke` line rather than on `REPORT`. The fix reads the
value from either line, still fails closed when neither carries a positive
value, and records which phase supplied it. That ~21 s init is itself an
operating-envelope fact for the [cold-start row](2026.1-operating-envelope.md#5-aws-lambda-serverless-supported-target-and-limits);
it is not yet a certified measurement.

## 4. What is blocking, right now

1. **honua-server [#4490](https://github.com/honua-io/honua-server/pull/4490) (open)** — the cold-start parse fix. Run 13 follows its merge.
2. **honua-iac [#175](https://github.com/honua-io/honua-iac/pull/175) (open, already applied)** — the substrate the last runs used is ahead of iac trunk. Until it merges, the certification account's state is not reproducible from a merged revision, and a fresh apply from trunk would not reproduce runs 10–12.
3. **Bill item 2 has never executed.** Reaching a passing receipt means the lane must get past the cold-start parse and then actually run the fixture row-count, write round-trip, denial and executed-version assertions against the cert PostGIS fixture.
4. **Items 3 and 4 cannot start** until a candidate exists; they are correctly parked in the `prove-against-candidate` bucket.
5. **The artifact under test is not the pinned one.** All twelve runs used the `3c4a8cd` trunk nightly (§3), not the manifest's `4ca8326` pin. Whoever dispatches run 13 must either pass the pinned `source_image` / `source_digest`, or the manifest must first be re-pinned to the artifact actually being certified. A green run on an unpinned artifact leaves item 1 open.

Nothing here is dispatched from this repository. Certification runs, AWS access,
and changes to honua-server or honua-iac belong to those lanes.

## 5. When the bill closes

Discharge, in order. Steps 2–4 all follow the candidate cut, and the manifest
must reach its final candidate state **before** the candidate-bound run, not
after it:

1. A green run of the lane dispatched against the **manifest-pinned** Lambda source digest — or against a manifest re-pinned to the certified artifact — with a receipt naming the exact source and ECR digests, executed serving assertions and cleanup outcomes → items 1 and 2. With item 5's pre-cut half already landed, the candidate cut is then unblocked.
2. Cut the candidate. Mirror the exact candidate's Lambda manifest into the release account, re-pin `awsLambdaEcrDigest` from that real mirror, and re-read `contractVersions` from the booted candidate → item 4. **The manifest is final at the end of this step.**
3. Re-run the lane against the exact candidate on that final manifest, with alias-shift upgrade and rollback through `AwsLambdaGitOpsDeployBackend`, serving verification before and after recovery, and full teardown → item 3.
4. Only then: `status: supported`, `qualification: passed`, and a `qualificationReceipt` whose `candidateManifestDigest` is `qualification_candidate_digest` over that same final manifest, and whose `url` is an immutable HTTPS location of **the step-3 receipt artifact itself** → item 5's remaining half completes and [release#282](https://github.com/honua-io/honua-release/issues/282) closes.

**Why item 4 precedes item 3.** `qualification_candidate_digest`
([`tools/validate_platform.py`](../tools/validate_platform.py), `:386-389`)
hashes the *entire* parsed manifest. Re-pinning `awsLambdaEcrDigest` or
`contractVersions` after the candidate-bound run therefore changes the digest
the receipt has to match, and the validator rejects the reference (`:428-429`).
Certifying first and re-pinning after would invalidate the receipt being bound;
the alternative is a second full certification run after the re-pin, which is
the same work done twice.

**Why the URL is the receipt, not the run page.** The
[operating envelope](2026.1-operating-envelope.md#5-aws-lambda-serverless-supported-target-and-limits)
requires the reference to point at the real full-bill receipt, but the validator
only checks that `url` has an HTTPS shape — scheme, host, non-empty path
(`:420-427`) — so nothing mechanical would stop a run page from passing while
identifying no evidence at all. A GitHub Actions run page is not the receipt
artifact: §6 shows the receipt only comes out of a separate artifact download,
and Actions artifacts expire on the repository's retention schedule. Publish or
archive `lambda-preview-receipt.json` at an immutable HTTPS URL and record that
URL; cite the run separately, as the reproduction path.

Any candidate-affecting change after step 3 invalidates the binding and requires
a fresh run and a fresh reference.

## 6. Reproducing this ledger

Read-only; requires `gh` authenticated against the honua-io org. No AWS access
is needed and none should be used from this repository.

```bash
# the twelve runs of this ledger, by ID — stable however many later
# certification attempts the lane accumulates
for id in 34052215320 34053195554 34053601724 34054001535 34064826386 \
          34065278695 34068427904 34069684026 34070228945 34078979087 \
          34079620255 34080138481; do
  gh run view "$id" -R honua-io/honua-server \
    --json databaseId,createdAt,headSha,conclusion
done

# anything the lane has run since this ledger's window closed
gh run list -R honua-io/honua-server \
  --workflow lambda-preview-certification.yml \
  --created '>=2026-09-07T03:36:00Z' \
  --json databaseId,createdAt,headSha,conclusion

# the source artifact a given run was dispatched against (§3)
gh run view <run-id> -R honua-io/honua-server --log \
  | grep -m1 HONUA_LAMBDA_SOURCE_DIGEST

# the receipt a given run produced
gh run download <run-id> -R honua-io/honua-server -D ./receipt
cat ./receipt/lambda-preview-certification-receipt/lambda-preview-receipt.json

# the lane's own contract, including the receipt schema
gh api repos/honua-io/honua-server/contents/scripts/cloud/lambda-certification.md \
  -H 'Accept: application/vnd.github.raw'

# the manifest's current Lambda declaration
python3 tools/validate_platform.py --exact-candidate   # expected: fails closed while qualification is pending
```
