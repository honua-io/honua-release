# SDK producer convergence — #181

Release promise: the 2026.1 supported platform set is certified against the exact
SDK producer revisions frozen in its manifest, using one immutable requirements
catalog and evidence ledger (quality contract §4.2, §9.2 and §14).

The pre-cut validator now rejects unequal or missing .NET, Python and JavaScript
catalog producer pins whenever `protocolCertification.ledger.status` is `bound`.
The same catalog commits must also equal the published package provenance in
`clientArtifacts.*.sourceSha`. Both the CLI and the Python entrypoint enforce
these rules. A pending working
snapshot remains structurally valid, but exact-candidate certification rejects it.
FINALIZE restores `bound` together with all three immutable binding coordinates.

The working snapshot's historical ledger is explicitly `pending`. Its prior
binding was evidence commit `c595f9d6c01e9e31aa46d460506950378f14ad85`, digest
`sha256:a2c8ae757e1d8671f037873605985a0446d66d75e09936632ffa73148a2d58c7`, and
requirements revision `47ec70604bb0db43b03f32dac1b114f1154bc2ca`. That ledger was
for the outgoing `e3ab87ce` candidate, not the current working snapshot. Live
repository variables still hold that historical triple; no coordinate was
advanced independently.

## Evidence collected 2026-09-06 UTC (2026-09-05 HST)

At release trunk `d02d459587844c99942a4854fe9e705d67e0f61b`:

| SDK | Manifest component source | Catalog producer source |
|---|---|---|
| .NET | `8e4dd3d9d23f86b7f07d946ef0736d4529d332b6` | same |
| JavaScript | `c99e71197dd940ed952aecb024c6de273456f2ae` | same |
| Python | `f7930b6e9c3ce47ade148bba3d4510eeffd2ccc4` | `516c727dc03cae3b6b312595a9f4bfcee5f34cad` |

The .NET package provenance is independently pinned to
`a88a7fbb3643cb046e70d6ef4d38ae70a025a2a4`, which also differs from its working
component/catalog revision. A bound ledger must converge all three identities;
certifying the working source alone cannot certify the published package.

The recovered validator failed on the committed Python mismatch before the
pending-state correction: 47 tests passed, one failed with both source SHAs in
the diagnostic.

The read-only plan command was:

```bash
python3 tools/convergence_rebind.py --plan-output /tmp/release-181-rebind-plan.json
```

After TLS timeout retries, it aborted on HTTP 404 for
[the Python certification contract at the frozen release source](https://api.github.com/repos/honua-io/honua-sdk-python/contents/conformance/protocol-certification.v1.json?ref=f7930b6e9c3ce47ade148bba3d4510eeffd2ccc4).
A direct GitHub contents request also returned 404, and `git show` against the
local immutable object confirmed that path does not exist at that commit. The
manifest identifies this source as the published PyPI 0.1.11 release. Substituting
a later producer SHA would misidentify those published bytes; copying the newer
contract under the older SHA would fabricate a source snapshot.

[Evidence aggregation run 34012339486](https://github.com/honua-io/honua-evidence/actions/runs/34012339486)
failed against release trunk `d02d459` because required producer
`server-protocol-harness` has no normalized fragments at catalog pin
`ac30266fbd153363bebdbed13130accc8ab0c94a`. Its governed candidate triple still
names the outgoing `e3ab87ce` image. Issue #181's earlier
[trunk-context producer attempt](https://github.com/honua-io/honua-server/actions/runs/33465402323)
also failed the actual protocol harness; a recent run from another revision
cannot substitute for evidence at the frozen pin.

## Remaining acceptance criterion

Released criterion in the delivery PR, because it requires the new exact
candidate: finish the source/catalog
convergence, re-aggregate the ledger against the staged honua-release commit,
and activate `PROTOCOL_CERTIFICATION_MATRIX_COMMIT`,
`PROTOCOL_CERTIFICATION_MATRIX_SHA256`, and
`PROTOCOL_CERTIFICATION_REQUIREMENTS_SOURCE_REVISION` together. Issue #181 stays
open in its existing must-fix-before-cut bucket until disposition is reconciled
by the release owner. This is not evidence of qualification.

This criterion requires the new freeze's exact published SDKs and candidate
image: the current Python artifact predates the mandatory producer contract,
and there is no complete ledger for the current denominator. Preserve the
freeze-at-cut rule and published artifact identities. At cut:

1. Select published SDK revisions containing their certification contracts and
   bind the manifest to those artifact identities. Select the new server image
   and run the required producers in trusted trunk context for that candidate.
   Recheck the gRPC/MCP producer-side image pins required by #181's checklist.
2. Run `convergence-rebind.yml` in PLAN mode, then APPLY after the sources and
   producer evidence exist. APPLY stages a regenerated catalog, dispatches
   aggregation for that exact staged commit and candidate triple, verifies the
   ledger digest, and finalizes a single review PR with the three gate pins.
3. Merge that rebind PR with a merge commit so the staged requirements commit
   remains reachable. `convergence-rebind-activate.yml` verifies ancestry and
   ledger identity, then copies all three coordinates from the merged manifest.
   Verify those variables and the strict evaluator on the resulting head.

## Regression evidence

The 2026-09-12 rebase onto trunk `cba728700f8ba50d8cad8df135999e955cae8985`
preserves the candidate pins and convergence golden values landed in #332 and
#339, plus the full-platform DR inventory from #334. The manifest conflict was
resolved by retaining the current server certification producer and combining
it with the explicit pending-ledger state and published-package explanation.
The pending ledger is the only manifest value change relative to that trunk.
Validation at this rebase: **257 passed** in the focused platform, convergence
rebind, deploy qualification, DR receipt, and platform-lock suites. Python
compilation and manifest validation against `origin/trunk` also passed.
The required full `python3 -m pytest tools/ -q` run passed **1,147 tests**
(229.57 seconds), with two Python tarfile deprecation warnings.

Initial focused suite: **83 passed** locally (333.78 seconds); full suite:
**757 passed** with localhost socket access (212.17 seconds). After the published-provenance
review fix, the platform suite passed **85 tests** (2.85 seconds), including the
historical .NET mismatch and wrong/missing provenance for each SDK. Re-run after
rebasing onto trunk (deploy-qualification checks now also run inside `validate()`):
**874 passed**, full `tools` suite, with the platform suite still at 85.

`python3 -m pytest tools/test_platform.py tools/test_convergence_rebind.py -q`
covers explicit equal producer pins; mismatches and missing/malformed producers
for all three SDKs; CLI rejection and default-catalog enforcement; pending state
rejection at exact-candidate certification; and pending/bound finalization with
assertions on ledger status, all three binding values, and all three gate pins.
The fixtures declare their expected commit values independently of validator
output. These are release-metadata checks; no geometry or raster execution is
claimed.
