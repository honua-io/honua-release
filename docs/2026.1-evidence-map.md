# 2026.1 GA evidence map — part 1

Owner: [release#210](https://github.com/honua-io/honua-release/issues/210) · audit run 2026-09-05, **re-verified against `honua-server` trunk `ac5723545a`** (2026-09-05 16:28 HST) · scope: priorities 1–3 (authorization and data disclosure; mutations, transactions, concurrency and recovery; geospatial correctness and the four cloud-native formats). Part 2 appends the remaining areas to this same document.

This is a **defensive review of what our own tests prove**, not a test inventory. It was produced by reading test bodies, fixtures and the code under test — never by trusting a test name, an `[Endpoint]` annotation, a workflow name, or a green run.

> **Audited revision.** Every row below is stated against `honua-server` **`ac5723545ac008b1f0357b35d0cbb8bfd67eff39`** and, where an SDK is cited, `honua-sdk-js` `b3ad28019`, `honua-sdk-python` `f2714f0`, `honua-sdk-dotnet` `a07d918`. The first pass of this audit read a local checkout that was 36 hours and 108 commits stale; every row and every filed issue was re-verified against `origin/trunk` afterwards, and the rows the re-verification changed are listed under Coverage limits. Trunk moves roughly twenty times a day, so a row is only as current as this revision.

## What the GA scope is

The authority is the [quality contract in `honua-flow`](https://github.com/honua-io/honua-flow/blob/8d5ae137efc0ca460afaf529f9fe390d949febc3/docs/2026.1-quality-contract.md) §14 plus every AMENDMENT line at the audit date, as adopted on release#210. For this packet that means:

- **GA:** core PostGIS-backed serving, auth/RBAC/isolation floor, selected OGC and GeoServices profiles, the **whole** `BuiltInProcessCatalog` (2026-09-04 reaffirmation, no per-operation carve-out), and **all four cloud-native formats** — COG, Zarr, GeoParquet, PMTiles (2026-09-04 decision 3). STAC and OGC API Records stay GA as the catalog surface.
- **Preview, isolation floor retained at full severity:** multi-tenancy (single-tenant is the GA posture), customer alerting, offline sync (`createReplica` / `synchronizeReplica` / `extractChanges`), ImageServer + its WMTS, OGC API EDR, OGC API Coverages, NAServer, VersionManagementServer.
- **Preview-tier evidence:** the SDK release certification lane (HTTP dev server, base seed); release-tier client evidence still needs the TLS + canonical-fixture overlay.

## Schema

Row data follows [`schemas/2026.1-evidence-map.schema.json`](../schemas/2026.1-evidence-map.schema.json). Each row is one GA operation-and-configuration claim, with five columns:

| Column | Meaning |
|---|---|
| **Contract** | operation · protocol · supported configuration · edition · principal. The exact thing we promise, for a named caller. |
| **Expected behaviour** | the exact result, the durable side effects, and the failure behaviour. |
| **Proof** | the responsible tests by path and test name, the fixture, the **execution layer**, and whether a **candidate-bound receipt** exists. |
| **Gaps** | untested behaviours, skipped cases, weak assertions, and evidence claims the tests do not support. |
| **Disposition** | keep / strengthen / consolidate / move / remove / not yet reviewed. |

**Execution layers** (the contract's five): `unit` · `api-integration` (API/HTTP integration, in-process or out-of-process; backing stores may be real or fake and are stated separately in each proof) · `published-container-e2e` · `emulator` · `real-ecs`.

**Candidate-bound receipt** means the evidence records the immutable candidate identity (image digest / lock) together with the required fixture and behaviour result. A passing local test, a scheduled workflow, a source SHA, or an API-surface inventory is **not** a receipt. In this packet the answer is `no` for every row; part 2 does not change that, because no pre-cut candidate exists yet.

**Structured representation:** `disposition` is a non-empty array of unique vocabulary values. An empty `proof.tests` requires `noProof: true`; populated tests forbid that state. A test with `candidateBoundReceipt: true` requires a `receipt` with an `imageDigest` or `lockSha256`, plus `fixtureSha256`, `resultSha256` and `result` (`pass`, `fail` or `skipped`). Each digest uses `sha256:` followed by 64 lowercase hex characters. These identify the candidate, fixture and behavior-result artifacts; consumers must retrieve and verify them. Schema validation alone neither authenticates evidence nor certifies a candidate.

**Disposition vocabulary:**

- **keep** — the test proves its contract row; leave it alone.
- **strengthen** — the behaviour is real and the test is pointed at it, but the assertion does not prove it. Never a deletion: a weak assertion on a critical behaviour is always strengthened.
- **consolidate** — several tests cover the same ground and one of them is the honest one; fold the others into it.
- **move** — the proof exists at the wrong layer (a unit seam standing in for a protocol round-trip, or vice versa).
- **remove** — the test asserts nothing about the product. Used once in this packet, on the operator's explicit 2026-09-04 ruling (a).
- **not yet reviewed** — inside scope but not read in this packet. Listed again under Coverage limits.

## Three facts that bound almost every row below

These were established from source, and they change how much most protocol tests prove. They are stated once here and referenced by rows rather than repeated.

**(F1) The integration fixture runs with authentication disabled by default.**
`tests/dotnet/Honua.TestKit/Mixins/WebAppFixturePostgresWiringMixin.cs::ApplyCommonHostSettings` sets `HONUA_DEV_AUTH=true` and `HONUA_DEV_AUTH_ALLOW_BYPASS=true` on **every** `WebAppFixture` host, shared and isolated alike (`WebAppFixtureSharedBootstrapMixin.cs` repeats it for the shared factory). In `src/Honua.Hosting/Features/Authentication/ApiKeyAuthenticationHandler.cs`, `HandleAuthenticateAsync` short-circuits on that bypass and returns a successful `"dev-bypass"` principal **before any credential is read**. A fixture opts back into real authentication by calling `ConfigureWebHost(b => b.UseSetting("HONUA_DEV_AUTH", "false"))`, which runs after `ApplyCommonHostSettings` and wins.

The numbers on trunk: **483** test files use `WebAppFixture`; **138 opt-out sites across 111 files**. Across the seven `Honua.Protocols.*.Tests` projects, 206 files use the fixture and 17 opt out; across the four core protocol projects it is 181 fixture files and **6** opt-outs (GeoServices 85/0, OgcApi 47/3, OgcClassic 20/1, OData 29/2). `Honua.Protocols.GeoServices.Tests` still has **zero** opt-outs; `Honua.Protocols.OgcClassic.Tests` gained its first one after this audit's first pass (`Wfs20EndpointsTests.cs:1125-1126`, [#4192](https://github.com/honua-io/honua-server/pull/4192)). Consequence: the large majority of protocol tests prove **functional behaviour only** and prove nothing about authorization, and a test on a non-opted-out fixture whose name says "Anonymous" is describing a client that is in fact authenticated. Rows below name the fixture so the distinction is never guessed.

**(F2) The fixture that does prove authorization does not touch the database.**
`tests/dotnet/Honua.TestKit/Helpers/ServiceRbacTestFixture.cs` sets `HONUA_DEV_AUTH=false` and installs a header-driven `TestAuthHandler`, so its decisions are real. But it builds on `TestWebApplicationFactory`, which replaces `IFeatureReader`/`IFeatureWriter`/`ITileProvider`/`IStreamingFeatureStore` with the in-memory `tests/dotnet/Honua.TestKit/Infrastructure/TestFeatureStore.cs`. Consequence: authorization is proven at the endpoint/middleware layer against a fake store. No test proves that a denied principal is refused **records that actually exist in PostGIS**.

**(F3) The required per-PR check does not run the integration suite.**
`.github/workflows/pr-gate.yml` and `.github/actions/lean-gate/action.yml` run five test steps: `Honua.Server.Tests --filter "Tier=Fast"` (`:238`), `Honua.Architecture.Tests` (`:254`), two named MCP classes in `Honua.Ai.Tests` (`:274`), `Honua.Server.Tests --filter "Category=Architecture"` (`:296`), and — added by [#4378](https://github.com/honua-io/honua-server/pull/4378) after this audit's first pass — `Honua.Worker.Gdal.Tests --filter 'Category=RasterExecutionProof'` against the production GDAL image (`pr-gate.yml:228-240`), plus a boot/migrations smoke. **No `Honua.Protocols.*.Tests` project runs on the required gate**, so everything below marked `api-integration` still runs only in the trailing full matrix (`ci.yml` has no `pull_request` trigger). That is the adopted eventual-quality landing model and is not itself a defect — but outside the raster-proof step, "the gate is green" never implies any row below is proven.

---

## Priority 1 — Authorization and data disclosure (REST, SDK, MCP; Community/Pro/Enterprise)

| ID / sev | Contract | Expected behaviour | Proof (fixture · layer · receipt) | Gaps | Disposition |
|---|---|---|---|---|---|
| **A-01** / P0 | Per-operation RBAC across FeatureServer `applyEdits`/`query`/`delete`, OData CRUD + `$batch`, OGC API Features CRUD, OGC API Tiles, gRPC `ApplyEdits`; Pro; granted / partially-granted / ungranted principals | A grant authorizes exactly its own operation and no other; a query-only grant cannot write; a grant scoped to another layer or service does not apply; absent a grant the coarse policy decides, unchanged. | [`CrossProtocolPermissionMatrixTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Server.Tests/Features/Security/CrossProtocolPermissionMatrixTests.cs) — `ApplyEdits_EveryPresentKind_RequiresItsMatchingGrant` (theory: 7 edit-kind combinations × grant-present/absent × FeatureServer and gRPC), `FeatureServerApplyEdits_QueryOnlyGrant_DeniesWrite`, `FeatureServerQuery_GrantScopedToOtherLayer_DeniesUngrantedLayer`, `ODataUpdate_InsertOnlyGrant_DeniesUpdate`, `ODataDelete_InsertOnlyGrant_DeniesDelete`, `ODataBatch_NoGrant_FallsBackToCoarseDeny`, `OgcFeaturesUpdate_InsertOnlyGrant_DeniesUpdate`, `OgcTilesRead_NoGrant_FallsBackToCoarseDeny` · ServiceRbacTestFixture, in-memory store (F2) · api-integration · no | Every deny case asserts a status code; none asserts that no record was disclosed or written, because the store is a fake (F2). Positive cases assert `200`/`201` but do not read the mutation back. | **keep** — this is the strongest authorization asset in the repo and the model for the rest of the table; strengthen separately with a PostGIS-backed variant |
| **A-02** / P0 | FeatureServer service-level RBAC: `applyEdits`, `append`, `calculate`, `createReplica`, `synchronizeReplica`, `unRegisterReplica`; GeoServices REST; Pro; reader / scoped `data-editor:<service>` / admin / credential-less | A read-only role is refused every write operation; a service-scoped editor is allowed on its own service and refused on another; refusal happens **before** the request body is processed. | [`ServiceRbacAuthorizationTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Server.Tests/Features/Security/ServiceRbacAuthorizationTests.cs) — `ApplyEdits_WithReadOnlyRole_ReturnsForbidden`, `ApplyEdits_WithScopedDataEditor_DeniesOtherService`, `CreateReplica_WithAnonymousClient_AndMalformedBody_ReturnsUnauthorizedBeforeBodyValidation`, `SynchronizeReplica_WithReadOnlyRole_AndMalformedBody_ReturnsForbiddenBeforeBodyValidation` (+ ~20 more) · ServiceRbacTestFixture, in-memory store · api-integration · no | Denials assert only the GeoServices error code. Allow cases assert `200` and never read back. `AssertGeoServicesErrorAsync(401, 499)` accepts either code — both are denials, so this one is acceptable, but it is the loosest assertion in the file. | **keep**; **strengthen** — add read-back on allow and a no-change assertion on deny |
| **A-03** / P0 | Layer-level read filtering: `getEstimates`, `queryDomains`, `relationships`, layer metadata; GeoServices REST; visible layer + role-gated hidden layer; Pro; `reader` (not granted the hidden layer) | Hidden layers, their domains and their relationships are absent from every metadata response, **and** their records cannot be read. | [`FeatureServerAccessFilteringTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Protocols.GeoServices.Tests/Source/FeatureServer/FeatureServerAccessFilteringTests.cs) — `ServiceGetEstimates_WithHiddenLayer_ReturnsAccessibleLayersOnly` (asserts exactly one layer, id = AlphaLayerId), `QueryDomains_UsesSchemaDomainsAndFiltersHiddenLayers` (asserts the hidden field name and hidden layer id are absent), `QueryRelationships_HidesRelationshipsToHiddenLayers` · ServiceRbacTestFixture, hidden/visible catalog, in-memory store · api-integration · no | Proves **metadata** filtering only. The suite never issues `GET /rest/services/{svc}/FeatureServer/{hiddenLayerId}/query` as `reader`, so the disclosure question the row exists to answer — can the denied principal read the hidden layer's rows? — is untested. | **strengthen** — [server#4386](https://github.com/honua-io/honua-server/issues/4386) |
| **A-04** / P0 | WFS 2.0 `Transaction` per-operation write authorization; WFS; PostGIS-backed layer; GA edition; insert-only / update-only grantee | An insert-only grantee is refused `Update`, `Replace` and `Delete` in a Transaction; `Replace` authorizes as `Update`; anonymous write succeeds only where `AllowAnonymousWrite` is set on the resource. | `CrossProtocolPermissionMatrixTests.cs` → `WfsTransactionWriteAuthorizationSeamTests.WfsTransactionSeam_InsertOnlyGrant_DeniesUpdate` / `_DeniesDelete` / `WfsTransactionSeam_InsertGrant_AllowsInsert` / `WfsTransactionSeam_UpdateGrant_AllowsUpdate` · synthetic `DefaultHttpContext`, no HTTP · **unit** · no. The enforcement point is `src/Honua.Protocols.OgcClassic/Wfs20/Services/Wfs20Handler.Transaction.cs::ValidateTransactionLayerWriteAccessAsync`. | The seam is proven; the **wiring is not**. The test file states the reason itself: "The ServiceRbacTestFixture cannot render a full WFS Transaction HTTP round-trip". Removing the `ValidateTransactionLayerWriteAccessAsync` call from the dispatcher would leave every test green. And `tests/dotnet/Honua.Protocols.OgcClassic.Tests` — 38 test files across WFS 2.0, WMS, WMTS, WCS, WPS — contains exactly **one** `Unauthorized` assertion, `Wfs20EndpointsTests.cs:1139` in `Wfs_GetFeature_KvpMultiQuery_ProtectedTypeChallengesAnonymous` (added by [#4192](https://github.com/honua-io/honua-server/pull/4192), and the only test in that project to opt out of the bypass). Every other file runs on `WebAppFixture` (F1), where the denial path is unreachable. | **move** + **strengthen** — [server#4387](https://github.com/honua-io/honua-server/issues/4387) |
| **A-05** / P0 | WFS `GetFeature` / `GetPropertyValue` read authorization; WFS; PostGIS layer; GA; principal without a read grant | A principal without read access receives no feature records and the layer is absent from `GetCapabilities`. | `WfsReadVisibilitySeamTests.WfsReadSeam_QueryGrant_OverridesCoarseDeny` / `_NoGrant_FallsBackToCoarseDeny` — these call `AccessPolicyHelpers.IsResourceAccessibleAsync(...)` directly and assert a boolean · **unit** · no | The one HTTP-level case that now exists, `Wfs_GetFeature_KvpMultiQuery_ProtectedTypeChallengesAnonymous`, asserts a 401 plus a `WwwAuthenticate` header and an `AccessDenied` body — not that zero `<wfs:member>` elements are returned, and it does not cover `GetCapabilities`. Same wiring gap as A-04, on the disclosure side. | **strengthen** — [server#4387](https://github.com/honua-io/honua-server/issues/4387) |
| **A-06** / P1 | WMS `GetCapabilities` / `GetMap` / `GetFeatureInfo`, WMTS `GetTile`, WCS `GetCoverage` authorization; OGC classic; GA; principal without read access | Denied callers get a protocol-shaped access-denied document and **no imagery, tiles, coverage bytes or feature attributes**. | [`ServiceRbacAuthorizationTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Server.Tests/Features/Security/ServiceRbacAuthorizationTests.cs) → `WmsServiceRbacTests.GetCapabilities_WithAnonymousClient_ReturnsWmsAccessDeniedException` (`:451`) — asserts HTTP **401** plus `text/xml` and a `ServiceExceptionReport` carrying `code="AccessDenied"` (`:463-465`). [#4239](https://github.com/honua-io/honua-server/pull/4239) inverted this from the previous HTTP 200 after the audit's first pass, on the reasoning that WMS 1.3.0 §7.3.3.4 defines the payload but does not turn a denial into a successful HTTP response · ServiceRbacTestFixture · api-integration · no | Two operations across all of OGC classic: this one and WFS `GetFeature` (A-05). `GetMap`, `GetFeatureInfo` (which returns attribute data), WMTS `GetTile` and WCS `GetCoverage`/`DescribeCoverage` have no authorization test at all. `GetFeatureInfo` is the data-disclosure path here. | **strengthen** — [server#4388](https://github.com/honua-io/honua-server/issues/4388) |
| **A-07** / P0 | Import over both customer surfaces: `POST /api/v1/admin/import/upload` and MCP `honua_ingest_dataset`; REST + MCP; Pro/Enterprise; five principal profiles including bearer-scoped admin | Only an admin-write principal imports; every other profile is refused **and produces zero create-or-replace effects**; MCP cannot acquire authority REST does not grant. | [`ImportAuthorizationParityTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Server.Tests/Import/ImportAuthorizationParityTests.cs) — `ImportAuthority_RealTransportTenCaseMatrix_MatchesRestAdminWriteContract` (2 surfaces × 5 profiles, deny cases assert `_effects.Should().BeEmpty(...)` and `DidNotReceive().ImportFileAsync(...)`), `BearerAdminGrant_ScopeCanOnlyNarrowBothImportTransports` · real HTTP transport, substituted `IFileImportService` · api-integration · no | The effect assertion is against a substituted import service, so it proves the authority gate rather than durable data. That is the right trade for an authorization row. | **keep** — this is the exemplar; every "strengthen" below means "look like this" |
| **A-08** / P1 | MCP catalog resolution and tool invocation; MCP; live catalog/resource policy; Pro; principal whose membership was revoked between resolution and call | The descriptor is re-authorized at call time, not granted by prior membership; a denied call reaches no provider and returns no records. | [`McpCatalogAuthorizationDenominatorTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Ai.Tests/Source/McpCatalogAuthorizationDenominatorTests.cs) — `EveryResolvedDescriptor_ReauthorizesAfterPrincipalRemoval_BeforeInvocation`, `DynamicCatalog_IsResolvedAgainAtCallTime_NotGrantedByPriorMembership`; `Source/Mcp/ServicePublishMcpAuthorizationTests.cs` · spy/fake descriptors · **unit** · no | These prove the re-authorization denominator, not the feature resource policy. The first of the two known defects on this path is now fixed: [#4255](https://github.com/honua-io/honua-server/issues/4255) (MCP feature queries skipped the REST resource-access gate) was closed by [#4346](https://github.com/honua-io/honua-server/pull/4346), which added `McpMapToolTests.cs:68::QueryFeatures_DeniedResourcePolicy_DoesNotReadFeatures` — it runs the identical `AccessPolicyHelpers.RequireResourceAccessAsync` check, asserts the MCP call returns `isError`, and asserts `DidNotReceive().CountAsync(...)` / `DidNotReceive().QueryAsync(...)`, with siblings for `DescribeLayer` and `ListLayers`. [#4251](https://github.com/honua-io/honua-server/issues/4251) (provider routing) is still open. Still missing: no test compares MCP output with REST output for the same principal. | **strengthen** — existing [#4255](https://github.com/honua-io/honua-server/issues/4255), [#4251](https://github.com/honua-io/honua-server/issues/4251); parity test tracked on [server#4389](https://github.com/honua-io/honua-server/issues/4389) |
| **A-09** / P1 | Admin and operations endpoints under `/api/v1/admin/*`; REST; Pro/Enterprise; credential-less caller | A credential-less caller is refused and no admin state is read or changed. | 13 sites assert `BeOneOf(HttpStatusCode.Unauthorized, HttpStatusCode.Forbidden)` — e.g. `Features/Admin/RowLevelSecurityEndpointsTests.cs:218`, `Features/Admin/OpsObservabilityEndpointsTests.cs:168/262/286/310`, `Features/Admin/FieldMaskPolicyEndpointsTests.cs:146`, `Features/Operations/OperateStatusEndpointsTests.cs:104` · api-integration · no | A 401-or-403 disjunction is an acceptable denial assertion. What is missing everywhere is the paired authenticated-success case on a non-empty fixture, so these tests cannot distinguish "correctly refused" from "endpoint is broken for everyone". | **strengthen** — [server#4390](https://github.com/honua-io/honua-server/issues/4390) |
| **A-10** / P0 | Endpoint checks that accept refusal alongside success; REST admin/import; Pro | — (this row is about the assertions themselves) | [`EndpointCoverageTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Server.Tests/Features/API/EndpointCoverageTests.cs) — `Admin_GetTables_ShouldReturnTableList` asserts `BeOneOf(OK, Unauthorized)`; `Import_UploadFile_ShouldAcceptValidFormats` asserts `BeOneOf(OK, Accepted, Unauthorized)`; `Import_GetJobStatus_ShouldReturnJobStatus` asserts `BeOneOf(OK, NotFound, Unauthorized)` · WebAppFixture · api-integration · no | Exactly the pattern the operator's 2026-09-04 ruling (a) names. Worse than it looks: under F1 the `Unauthorized` branch is unreachable, so the disjunction is dead cover for whatever the endpoint actually did. `Import_UploadFile_ShouldAcceptValidFormats` posts a GeoJSON feature and never checks that anything was imported. Real import authority and fidelity are proven elsewhere (A-07, and the `tests/dotnet/Honua.Server.Tests/Import/` suite), so these three are redundant as well as weak. | **strengthen / consolidate** into the Import suite — [server#4390](https://github.com/honua-io/honua-server/issues/4390) |
| **A-11** / P2 | Test-suite quality reporting (no product contract — this row is about a test that measured the suite, not the server) | A check that reports on the test suite must measure something a regression would change; an aggregate score derived from token counts must not exist. | **Resolved.** `TestQualityMetrics_AllCriteria_AchievesPerfectScore` is absent from trunk (`git grep AchievesPerfectScore origin/trunk -- tests/` returns nothing), removed by PR [#4357](https://github.com/honua-io/honua-server/pull/4357) (`643d113fed`). `tests/dotnet/Honua.Server.Tests/Comprehensive/TestQualityValidationTests.cs` retains only the honestly-named structural checks. · — · n/a | The test globbed `tests/**/*.cs` and counted token occurrences — `[UnitTest]` ≥ 100, the words `Invalid`/`Malformed`/`Empty` ≥ 250 "edge-case indicators", `Throws`/`Fails`/`Denied` ≥ 150 "error-path indicators" — then reported a 100/100 quality score. Adding the word "Invalid" to a comment moved the metric; deleting a real assertion did not. | **remove — done.** Operator ruling 2026-09-04(a) is discharged; [server#4391](https://github.com/honua-io/honua-server/issues/4391) was filed against the stale checkout and is closed as not planned |
| **A-12** / P0 | Cross-tenant isolation on every surface where tenancy exists (Preview surface, floor retained at full severity); all protocols + MCP; trial only; two tenant principals | A request bound to tenant A never reads or mutates tenant B, and never discloses B's existence. | No cross-protocol isolation matrix exists. The known defects are filed: distinct tenant IDs collapsing onto one derived schema ([server#4266](https://github.com/honua-io/honua-server/issues/4266)), GP job ownership ignoring the effective tenant ([#4289](https://github.com/honua-io/honua-server/issues/4289)), admin mutation audit omitting the tenant ([#4262](https://github.com/honua-io/honua-server/issues/4262)), proposal/handle reads lacking ownership checks ([#4256](https://github.com/honua-io/honua-server/issues/4256)), sealed tenant lost at HTTP auth ([#4257](https://github.com/honua-io/honua-server/issues/4257)) · — · no | The floor is stated at full severity but has no standing test asset: no fixture with two populated tenants and asserted IDs. | **strengthen** — existing first-release-gate issues above; no new issue filed (the defect set is already tracked and the fixture work belongs to them) |
| **A-13** / P0 | OGC API Features write authentication: `POST`/`PUT`/`DELETE` items and batch; OGC API; real PostGIS, `HONUA_DEV_AUTH=false`, admin API key; Pro; credential-less caller | A credential-less write is refused, **changes nothing**, and the error body discloses nothing. | [`OgcFeaturesAuthorizationTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Protocols.OgcApi.Tests/Source/Features/OgcFeaturesAuthorizationTests.cs) — `CreateFeature_WithoutApiKey_ReturnsUnauthorized`, `UpdateFeature_…`, `DeleteFeature_…`, `Batch_WithoutApiKey_ReturnsUnauthorized`, and the positive `CreateFeature_WithApiKey_ReturnsCreated` · isolated host, **real authentication**, real PostGIS · api-integration · no | Genuine authentication proof — and each of the four denials is one line, `StatusCode.Should().Be(Unauthorized)`. No count-before/after, no read-back, no body check. The positive case asserts `201` and never reads the feature back. **There is no GET in the file at all**: unauthenticated `GET …/items` is untested, so the read side of the disclosure promise is unproven on this protocol. | **strengthen** — [server#4392](https://github.com/honua-io/honua-server/issues/4392) |
| **A-14** / P0 | OData write authentication: create / update / delete, including malformed bodies; OData; real PostGIS, `HONUA_DEV_AUTH=false`; Pro; credential-less caller | As A-13, and malformed unauthorized requests are refused before body processing. | [`ODataAuthorizationTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Protocols.OData.Tests/Source/ODataAuthorizationTests.cs) — five tests, each `StatusCode.Should().Be(HttpStatusCode.Unauthorized)` · isolated host, real authentication, real PostGIS · api-integration · no | Same shape as A-13: no state check, no body check, and `GET /odata/Layers(0)/Features` unauthenticated is untested. | **strengthen** — [server#4392](https://github.com/honua-io/honua-server/issues/4392) |
| **A-15** / P0 | Denied read returns no records; OGC API Features, OData, FeatureServer; Pro; principal denied on a collection that holds rows | The response carries zero records from the denied collection, and an allowed response never contains rows from a denied one. | `CrossProtocolPermissionMatrixTests` deny cases route through `ServiceRbacTestFixture.AssertStatusAsync` (`ServiceRbacTestFixture.cs:169-173`), which is `response.StatusCode.Should().Be(expected, body)` — the body is only a failure message, never asserted on. The allow-side reads (`OgcFeaturesRead_QueryGrant_OverridesCoarseDeny`, `ODataQuery_QueryGrant_OverridesCoarseDeny`) assert `OK` and never inspect the features. `ODataErrorHandlingTests.Batch_MixedAccess_ReturnsPerItemResponses` is the closest cross-collection case and asserts only `responses[0]=200, responses[1]=403`. · in-memory store · api-integration · no | **No test anywhere asserts that a denied principal received zero feature records.** Every RBAC denial in the repo is a status code. Two allow-side write cases assert `NotBe(Forbidden)` + `NotBe(Unauthorized)` — the "not 403" pattern — with a code comment conceding the in-memory fixture cannot complete the create. | **strengthen** — [server#4386](https://github.com/honua-io/honua-server/issues/4386) |
| **A-16** / P1 | SDK auth and query/mutation paths (JS, Python, .NET) against the candidate | The SDK sends the right credential, receives the exact server result, and preserves the server's denial and no-disclosure semantics. | see **M-17** in Priority 2 | Mocked transport in bulk; the real-server lanes are digest-pinned but plain HTTP, and no SDK test in any language asserts a 401 or 403 from a real server. | **strengthen** — see M-17 |
| **A-17** / P1 | Edition gates on licensed operations; Community / Pro / Enterprise | Community refuses licensed operations with the documented payment result; licensed editions execute; the strict license failure-mode contract (2026-09-05) holds. | see **M-19** (edition gates) and **M-20** (the strict license failure-mode contract, implemented on trunk by #4358) | The 402 sweep proves gate wiring against an in-memory entitlement service, not the license lifecycle behind an HTTP request. | **keep** — see M-19, M-20 |

---

## Priority 3 — Geospatial correctness and the four GA cloud-native formats

Cloud-native format serving — COG, Zarr, GeoParquet, PMTiles — is GA by the 2026-09-04 decision 3, which also makes every finding of the cloud-native hunt on those surfaces first-release-gate must-pass. The rows below are about **evidence**, not those defects.

One fact frames the whole section: **`docs/cng-status.md:67-75` states that Honua does not author COG and only reads/transcodes Zarr.** Any GA row that reads as "Honua produces a COG" is a claim the product does not make. The GA claim is *serving from* and *reading* these formats.

### Query and filtering correctness

| ID / sev | Contract | Expected behaviour | Proof (fixture · layer · receipt) | Gaps | Disposition |
|---|---|---|---|---|---|
| **G-01** / P1 | FeatureServer `query`: `where`, `objectIds`, point+`distance` buffer, `returnExtentOnly`, quantization; GeoServices REST; five-feature PostGIS seed with matching and non-matching rows; all GA editions | Filtering returns exactly the matching ids and excludes the non-matching; extent and count are numerically correct; a zero-distance buffer does not match offset features. | [`FeatureServerQueryGapsTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Protocols.GeoServices.Tests/Source/FeatureServer/FeatureServerQueryGapsTests.cs) — `Query_WithReturnExtentOnly_ReturnsExtentAndCount` (asserts `Count == 5` and the envelope to ±0.01), `Query_WithPointAndDistanceBuffer_MatchesNearbyFeatures`, `Query_WithPointAndZeroDistance_DoesNotMatchOffsetFeatures`, `Query_WithQuantizationParameters_EmitsTransformAndIntegerCoordinates` · `WebAppFixture`, real PostGIS, auth bypassed (F1) · api-integration · no | Real PostGIS and a real seed — this is honest spatial evidence. The positive `where` case asserts extent and count rather than the matching object ids, and the service-level `layerDefs` case only covers the empty result. | **keep**; **strengthen** the `where`/`layerDefs` id assertions |
| **G-02** / P1 | OGC API Features `datetime` filtering; OGC API; `tests/seed/server.yaml` layer 0 (ids 1-5, matching and non-matching) | Exactly the temporally matching ids are returned. | `OgcFeaturesEnhancementsTests.GetItems_WithDateTimeParameter_FiltersFeatures` — `ids.Should().Equal(1, 2, 4);` · real PostGIS, auth bypassed · api-integration · no | None material. This is the best filtering assertion in the OGC/OData surface and the shape every other filtering row should copy. | **keep** — reference pattern |
| **G-03** / P1 | OGC API Features `bbox` filtering; OGC API; seeded layer with geometries inside and outside the box | A feature outside the bbox is excluded and one inside is returned, by id. | `OgcFeaturesItemsTests.GetItems_WithValidBbox_ReturnsFilteredFeatures` and `GetItems_With3dBbox_AcceptsAndUsesHorizontalExtent` both request the **worldwide** bbox `-180,-90,180,90` and assert only `TryGetProperty("features", out _).Should().BeTrue()`; `GetItems_WithDatelineCrossingBbox_ReturnsResults` asserts `NotBeEmpty()`; `OgcFeaturesSpatialReferenceTests.GetItems_WithProjectedBboxCrs_AllowsProjectedRange` asserts only `OK` · api-integration · no | **No OGC API Features test excludes a geometry-bearing feature by location.** The one bbox exclusion test, `GetItems_WithBbox_ExcludesNullGeometryFeatures`, excludes on null geometry, not position. A bbox filter that was ignored entirely would pass every one of these. | **strengthen** — [server#4393](https://github.com/honua-io/honua-server/issues/4393) |
| **G-04** / P1 | OGC API Features CQL2 filtering; OGC API; seed with `category ∈ {test(1,3,5), sample(2,4)}` | Exactly ids {1,3,5} come back for `category='test'`. | `OgcFeaturesItemsTests.GetItems_WithCqlFilter_ReturnsFilteredFeatures` — the test's own comment says *"may be empty if no matches"* and its assertion is a `foreach` over the result, so **it passes on zero features**; `GetItems_WithComplexCqlFilter_ReturnsFilteredFeatures` asserts only `type == "FeatureCollection"` and that a `features` key exists; `GetItems_WithAllParameters_…` asserts `Length ≤ 1` and never checks the filter. `GetItems_WithCql2JsonFilter_ReturnsFilteredFeatures` is better — `NotBeEmpty()` plus a per-row `category == "test"` — but asserts no count or id set. · api-integration · no | Three of four CQL tests are vacuous or shape-only on a fixture that makes the exact assertion available. The CQL2-JSON test proves no false positives but not the absence of false negatives. | **strengthen** — [server#4393](https://github.com/honua-io/honua-server/issues/4393) |
| **G-05** / P1 | OData `$filter`, `bbox` shorthand, `$orderby`, `$top`/`$skip`; OData; 15-row seed with matching and non-matching rows | Exact counts and ids. | [`ODataFilterMatrixTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Protocols.OData.Tests/Source/ODataFilterMatrixTests.cs) — `ContainSingle().Which…ObjectId == 1`, `HaveCount(5)` capitals / `HaveCount(10)` non-capitals, `NotContain(ObjectId == 1)`; [`ODataBboxShorthandTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Protocols.OData.Tests/Source/ODataBboxShorthandTests.cs) — `Contain(SanFranciscoId)`, `Contain(SacramentoId)`, `NotContain(LosAngelesId)`, `NotContain(SeattleId)`, and an attribute∩bbox intersection case · shared server, real PostGIS · api-integration · no | Strong. The weak sibling is `ODataFilterFunctionTests` (`length`, `indexof`, `round`, `floor`, `ceiling`, `abs`): all seven assert `NotBeEmpty()` plus a per-row re-check of the same predicate, so a filter silently dropping half the matching rows passes. | **keep** the matrix and bbox suites — reference pattern; **strengthen** `ODataFilterFunctionTests` |
| **G-06** / P1 | CRS, axis order and geometry precision; GeoServices / WFS / OGC API; EPSG:4326 and 3857 fixtures | Coordinates, axis order, SRID, bbox and distance semantics are exact; no silent reprojection or swapped axes. | `Wfs20EndpointsTests.Wfs_GetFeature_BboxWithoutSrsName_UsesLayerAxisOrder`, `Wfs_Transaction_Insert_3dGmlPosList_EvenVertexCount_StoresCorrectGeometry`; `FeatureServerQueryGapsTests.Query_WithQuantizationParameters_EmitsTransformAndIntegerCoordinates`; `GeoParquetSdkInteropConformanceTests` checks emitted axis order against PROJJSON · api-integration + unit · no | Protocol-local fixtures with selected assertions. No independent client comparison of the same geometry across two protocols, and no candidate-bound observation. | **strengthen** (part 2: cross-protocol round-trip, owned by release#157) |

### COG (GA)

| ID / sev | Contract | Expected behaviour | Proof (fixture · layer · receipt) | Gaps | Disposition |
|---|---|---|---|---|---|
| **G-07** / P1 | COG tile decompression: LZW (pred 1/2, uint8/uint16/RGB/multi-tile), ZSTD, DEFLATE, NONE | Decoded tile bytes equal GDAL's own decode of the same file. | [`TileDecompressorFixtureTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Core.Tests/Raster/CogParser/TileDecompressorFixtureTests.cs) — 10 theory variants over real GDAL 3.12.1 `.tif` fixtures generated by `scripts/raster/generate-cog-fixtures.py`, `AssertBytesEqual(expected, actual, fixture)` against a checked-in `.bin` from GDAL; **no skip conditions** — a missing fixture throws · unit · no | None material at this layer. | **keep** — the single best piece of evidence in this audit |
| **G-08** / P1 | COG metadata extraction and bounded reads | Extent, CRS, overviews and IFD fields decode exactly; a hostile header cannot force an unbounded read. | `CogMetadataExtractorTests`, `CogRasterHeaderProbeTests`, `TiffIfdParserTests`, `TilePredictorTests`, `CatalogRasterSourceResolverTests` — real field oracles (`Extent.XMin.Should().BeApproximately(-10.0, 1e-10)`) and fail-closed guards (`Assert.Equal(0, reader.UnconditionalReadCount)`) over `InMemoryRangeReader` · unit · no | Fake range reader; no object-store boundary. | **keep** |
| **G-09** / P0 | Serving tiles from a registered cloud-hosted COG; ImageServer tile path; Pro/Enterprise; authorized reader | A registered COG is range-read and the served tile matches the source pixels. | `CogEndpointTests.cs:158::CogCrud_RegisterListGetDelete_Lifecycle` — metadata CRUD only; `bucket`/`objectKey` are strings in Postgres rows and **no bytes are fetched** (`getDoc` at `:196` echoes `objectKey`). `CogTileResolverTests` substitutes `ICloudRangeReader`, `ICogStore` and `ICogMetadataReader`, and its "tile" is `{0xFF,0xD8,0xFF,0xD9}` asserted by pass-through. `CogImportEndpointTests` asserts `NotBe(UnsupportedMediaType)` with a comment conceding the synthetic TIFF may 500. · unit / api-integration · no | The serving path is `ICogTileResolver` injected into `src/Honua.Protocols.GeoServices/ImageServer/Handlers/ImageServerTileHandler.cs:38`, and `ImageServerTileHandlerTests` / `ImageServerExportHandlerTests` have **zero** occurrences of `Cog` or `GeoTiff`. **No HTTP-level test serves a COG-derived tile.** No test uses a real object store — `git grep -l -E "Azurite\|MinIO\|LocalStack\|EmulatorFixture" origin/trunk -- 'tests/*.cs'` returns 15 files, none of them a COG test. No 206/`Content-Range` test exists for COG; the repo-wide range grep returns 19 hits in 5 files, none for a cloud-native format other than PMTiles. | **strengthen** — [server#4394](https://github.com/honua-io/honua-server/issues/4394); existing [#3377](https://github.com/honua-io/honua-server/issues/3377) |

### Zarr (GA)

| ID / sev | Contract | Expected behaviour | Proof (fixture · layer · receipt) | Gaps | Disposition |
|---|---|---|---|---|---|
| **G-10** / P1 | Zarr chunk decode: uncompressed, zlib, v3 gzip, fill value; chunk/index/axis semantics | Decoded cell values equal a computed oracle; decompression bombs are refused. | [`ZarrSubsetReaderTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Core.Tests/Raster/ZarrParser/ZarrSubsetReaderTests.cs) — `var expected = (float)globalRow * 100 + globalCol; … actual.Should().Be(expected)`, plus BH-017 / BH2-015 / BH3-022 security regressions · `InMemoryZarrRangeReader` · unit · no | Fake store only. | **keep** |
| **G-11** / P0 | Serving a registered Zarr through WCS 2.0, ImageServer `exportImage` and the datacube tile route; Pro/Enterprise; authorized reader | The served raster reflects the registered Zarr's chunk/index/axis/CRS/nodata semantics; oversize and failure leave no partial result. | `Wcs20ZarrEndpointsTests.Wcs_GetCoverage_NumericImageServerRoute_ReturnsRegisteredZarrSlicePng` (+ ServiceRoute twin, + both `Wcs20ZarrTrimEndpointsTests` methods) assert **PNG magic bytes only**: `ContentType == "image/png"` and `bytes.Should().StartWith([0x89,0x50,0x4E,0x47,…])`. `ImageServerBasicTests.ExportImage_WithMultidimensionalDefinition_ReturnsNativeSlicePng` — status and content-type only. `ZarrTileRendererTests` checks PNG IHDR width/height, never pixels — and its second test, `Render_WithColormap_MapsValuesToColours`, builds a two-entry colormap then asserts only the PNG signature and `png.Length > 8`, so the colour mapping its name promises is never checked. The real value checks are `ImageServerBasicTests.Identify_WithValidPoint_ReturnsIdentifyResult` (`value == "1022"`) and `ImageServerSamplesHandlerTests.GetSamplesAsync_WithVerticalSlice_SamplesZarrStore` (`body.Should().Contain("1022")`) — both on a fake `ICloudRangeReader`. · api-integration · no | **The datacube tile route `GET /api/v1/datacubes/{layerId}/tiles/{tms}/{z}/{x}/{y}` has two tests and both assert a negative status** — `DatacubeTile_LayerWithoutServableCoverage_Returns404` and `DatacubeTile_WithoutAuthentication_Returns401BeforeStoreLookup`, the latter added by [#4249](https://github.com/honua-io/honua-server/pull/4249). There is still no positive HTTP test of the route. Every Zarr backing store in tests is a `Dictionary<string,byte[]>` behind `ICloudRangeReader` — `InMemoryZarrRangeReader.cs:17`, `Wcs20ZarrEndpointsTests.cs:266`, `Wcs20ZarrTrimEndpointsTests.cs:197`, `Wcs20ZarrOversizeEndpointsTests.cs:148`, `ImageServerZarrTestFixture.cs:52`. No HTTP 206/range test; the only range instrumentation is `TrackingZarrRangeReader` (`ZarrSubsetReaderTests.cs:518`), which asserts chunk read-length bounding against an in-memory dictionary, not `Content-Range` semantics. `CuratedCorpusTests.ZarrAsset_IsARealTwoSliceFloat32Cube` reads the real committed cube but decodes it with its own `BitConverter`, never through `ZarrMetadataExtractor`/`ZarrSubsetReader` — it proves the fixture, not the server. | **strengthen** — [server#4395](https://github.com/honua-io/honua-server/issues/4395); existing [#3377](https://github.com/honua-io/honua-server/issues/3377) |
| **G-12** / P1 | GDAL multidimensional scan → materialized Zarr metadata | A real worker scan produces metadata the server materializes correctly. | `MultidimensionalCoverageEndpointTests.ScanJobStatus_SucceededJob_MaterializesMetadata` **fabricates the worker output as a string literal** (`var envelope = """{"mdiminfo":""" + GdalMdimInfoJson + …`) and seeds the job record as `Succeeded`. The real chain runs only in `docker/client-compat/multidim-fixture/verify.py` (real LocalStack S3, real NetCDF, real GDAL worker), whose assertions are "a `.zarray`/`zarr.json` key exists", "the bytes start with the PNG signature", and "two time slices are not byte-identical" · emulator · no | The unit test proves the materializer against a hand-written envelope, not against GDAL. The emulator lane is real but has no pixel oracle, runs on `schedule: '15 8 * * *'` in `multidim-raster-fixture.yml`, and its `workflow_dispatch` takes **no inputs**, so digest binding is impossible. | **strengthen** — [server#4395](https://github.com/honua-io/honua-server/issues/4395) |

### GeoParquet (GA)

| ID / sev | Contract | Expected behaviour | Proof (fixture · layer · receipt) | Gaps | Disposition |
|---|---|---|---|---|---|
| **G-13** / P1 | GeoParquet output metadata conformance | Emitted `geo` metadata conforms to GeoParquet 1.1.0 + PROJJSON; axis order and bbox match the geometry. | [`GeoParquetSdkInteropConformanceTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Protocols.GeoServices.Tests/Source/FeatureServer/Services/GeoParquetSdkInteropConformanceTests.cs) — JSON-Schema validation against the bundled `geoparquet-v1.1.0.schema.json` with an offline `JSchemaPreloadedResolver`, plus independent `ParquetSharp.Arrow.FileReader` + `WKBReader` decode; `GeoParquetQueryFormatterTests` asserts per-column values (`point.X.Should().Be(1.23)`) · in-process, no HTTP · unit · no | Not exercised over HTTP. | **keep** |
| **G-14** / P1 | GeoParquet served over HTTP and read by an independent consumer; FeatureServer / OData; Pro | A live server's `f=parquet` / `$format=parquet` response is a valid GeoParquet file whose records and CRS match a GeoJSON query of the same layer. | [`tests/python/feature_server/test_geoparquet_interop.py`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/python/feature_server/test_geoparquet_interop.py) — live server process + Testcontainers PostGIS, read by `pyarrow`/`geopandas`/`pyproj`, cross-checked against a live GeoJSON query: `sorted(gdf["name"].tolist()) == reference_names`, `gdf.crs.to_epsg() == 4326` · api-integration (out-of-process) · no | Two disqualifiers. (1) It **self-skips**: `_skip_if_parquet_unavailable` calls `pytest.skip` on HTTP 404 or 501 ("native Parquet writer unavailable on musl images") at the top of every test, so a missing writer is green. (2) It runs only in `ci.yml`'s python job gated on `full_ci == 'true'`, in a workflow with no `pull_request` trigger, and builds the server from the checkout — no candidate image, no digest. | **strengthen** — [server#4396](https://github.com/honua-io/honua-server/issues/4396) |
| **G-15** / P1 | GeoParquet over OData `$format=parquet` | The response decodes to the filtered rows. | `ODataParquetFormatTests` — real Testcontainers PostGIS and a real HTTP round-trip, then `Encoding.ASCII.GetString(payload,0,4).Should().Be("PAR1")` and the same at the tail · api-integration · no | Magic bytes only: no `geo` metadata, no geometry, no record values, on a test that already paid for a real database and a real request. `GeoParquetImportTests` verifies import by response substring `"featureCount":1`. | **strengthen** — [server#4396](https://github.com/honua-io/honua-server/issues/4396) |

### PMTiles (GA)

| ID / sev | Contract | Expected behaviour | Proof (fixture · layer · receipt) | Gaps | Disposition |
|---|---|---|---|---|---|
| **G-16** / P1 | PMTiles archive publication and HTTP range access | A published archive is durable, range-readable at any offset, and decodes in required clients. | [`PMTilesPublishEndpointTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Server.Tests/Features/Admin/PMTilesPublishEndpointTests.cs) — `PMTilesProxy_RangeRequest_Returns206WithRangeHeaders` is the only genuine 206 / `Accept-Ranges` / `Content-Range` test for any of the four formats; `PMTilesArchiveEndpointTests.CompletedArchiveJob_ProducesValidPMTilesArchive` downloads the real archive from `LocalFileStorage` · real ASP.NET host + Testcontainers Postgres + real local file storage · api-integration · no | The range test only requests bytes 0-6 — the `PMTiles` magic prefix. No interior or tile-offset range is exercised over HTTP; `PMTilesProxy_UnsatisfiableRange_Returns416` asserts the status only. (Interior ranges *are* covered at the service layer — `PMTilesProxyServiceTests.cs:193` `bytes=2-2` and `:229` `bytes=2-5` — but against a stub store, not over HTTP and not at a real tile offset.) The archive check hand-parses fixed header offsets with `BitConverter` (`tileType == 1`, `rootDirOffset == 127`, length == `totalSize`) and never decompresses the directory or reads a tile. | **strengthen** — [server#4397](https://github.com/honua-io/honua-server/issues/4397) |
| **G-17** / P1 | PMTiles tile retrieval and decode | A tile fetched by z/x/y decompresses to the expected MVT bytes. | `PMTilesDirectoryTests.WriteAsync_DirectoryEntriesLocateTileData` is the fullest round-trip — but through Honua's own `PMTilesHeader.ReadFrom` / `PMTilesDirectory.Decompress` / `HilbertCurve`, with `PMTilesCompression.None`, 3 tiles (root directory only, no leaf path), matching expected tiles **by length, not coordinate**. `PMTilesWriterTests.WriteAsync_TileDataIsRetrievable` seeks by header offset and compares `Random.Shared.NextBytes` payloads. `TileOperationJobServicePublishTests` uses a `StubCloudStorage` whose downloads are hardcoded to `null`, so readback is architecturally impossible there; its tiles are `{0x01,0x02,0x03,0x04}`. · unit · no | No test decompresses a **gzip-compressed** tile and compares bytes; `git grep -i PMTilesCompression.Gzip origin/trunk -- tests/` returns three hits, all inside `PMTilesWriterTests.WriteAsync_WithGzipCompression_SetsHeaderCorrectly`, which asserts header fields only. [#4247](https://github.com/honua-io/honua-server/pull/4247) added five PMTiles tests after this audit's first pass; all are layout, streaming or metadata cases, none decodes a tile or goes over HTTP. No leaf-directory path is exercised. No test reads real MVT out of an archive. | **strengthen** — [server#4397](https://github.com/honua-io/honua-server/issues/4397) |
| **G-18** / P1 | PMTiles consumed by a required client (browser / MapLibre `pmtiles://`, go-pmtiles, Python/Node readers) | An independent reader opens what the **server** serves and renders the expected tiles. | The CNG lane's `pmtiles verify` (go-pmtiles 1.30.0), Python `pmtiles` 3.7.0 and Node `pmtiles@4.5.0` all read the **local** `honua.pmtiles` file produced by `scripts/conformance/cng/artifact-gen`, whose tiles are synthetic `MakeTilePayload(seed)` bytes, not MVT. `grep -rn "pmtiles" tests/js-browser/ tests/js/` → no matches. · — · no | Nothing reads what `/api/v1/tiles/pmtiles/{artifactId}` actually serves. No browser test exists. | **strengthen** — [server#4397](https://github.com/honua-io/honua-server/issues/4397) |

### The CNG conformance lane itself

| ID / sev | Contract | Expected behaviour | Proof | Gaps | Disposition |
|---|---|---|---|---|---|
| **G-19** / P0 | Cloud-native conformance evidence for all four GA formats | The lane produces passing, candidate-bound observations that a GA claim can cite. | `scripts/conformance/cng/run-cng-conformance.sh`, `validate-canonical-artifacts.py`, `.github/workflows/cng-conformance.yml` | Three compounding problems. **(1) The COG and Zarr artifacts are not Honua's.** `generate-canonical-fixtures.py` produces `canonical.cog.tif` with `rio_cogeo.cog_translate` and `canonical.zarr` with `xarray.to_zarr`; `validate_cog` and `validate_zarr` then read them with rasterio / zarr / xarray. **Zero Honua code is in that loop**, and the script says so itself. Only `cng.parquet` and `cng.fgb` come from the live server over HTTP; `honua.pmtiles` comes from `artifact-gen` as a local file and is never served. **(2) The fragment contains no passing rows.** `validate-canonical-artifacts.py:443-447` rewrites every `pass` to `skip` with `BUDGET_EVIDENCE_GAP`, nulls the `evidence_digest`, and emits `"operation_scope": {"complete": false, "owner_issue": ".../issues/3377"}`; COG, Zarr and HDF5 are additionally `_mark_unbound` (defined at `:395-400`, applied at `:733-735`) with `UNBOUND_CONSUMER_GAP`. All 23 governed assignments are `"result": "skip"`. **(3) The declared oracles are never evaluated.** `FORMAT_BUDGET_PROFILES` defines COG `expected_metadata` (`crs EPSG:4326`, `dimensions [256,256]`, `nodata -9999.0`, `overview_count 3`), GeoParquet (`feature_count 6`, bounds), PMTiles (`spec_version 3`, `tile_count 21`), Zarr (`shape [4,8,16]`, `chunks [1,4,4]`) and every `min_range_requests` / `max_full_object_downloads` budget — and nothing reads a profile to compare anything. The only consumer is a self-test asserting the profiles exist. The validator's own unit test `test_validate_canonical_artifacts.py` is not in `tests/python/pytest.ini` `testpaths` and no workflow runs it. The workflow is `schedule` (weekly Wed 06:00 UTC) + `workflow_dispatch`; it *can* require an `image@sha256` digest, but nothing in the repo dispatches it, and on the schedule it uses a local `docker image inspect --format '{{.Id}}'` value. | **strengthen** — [server#4398](https://github.com/honua-io/honua-server/issues/4398); existing [#3377](https://github.com/honua-io/honua-server/issues/3377) |

## Priority 2 — Mutations, transactions, concurrency and recovery

| ID / sev | Contract | Expected behaviour | Proof (fixture · layer · receipt) | Gaps | Disposition |
|---|---|---|---|---|---|
| **M-01** / P1 | FeatureServer `applyEdits` (service and layer level), `addFeatures`, `updateFeatures`, `deleteFeatures`; GeoServices REST; seeded PostGIS; Pro; authorized editor | Valid edits persist and read back with exact values; an invalid edit returns the documented error and leaves the prior record untouched. | [`FeatureServerMutationScenarioTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Protocols.GeoServices.Tests/Source/FeatureServer/FeatureServerMutationScenarioTests.cs) — `MutationEndpoints_AcceptValidEdits_AndRoundTripEachState` (reads back through the API and asserts the exact `name`, and `ReadFeatureCountAsync(objectId) == 0` after delete), `MutationEndpoints_RejectInvalidEdits_WithoutChangingStoredState` (`ReadCountByNameAsync("invalid-add") == 0`, `AssertFeatureNameAsync(originalId, "mutation-original")` after a failed update and after a malformed delete) · `WebAppFixture`, real PostGIS, auth bypassed (F1) · api-integration · no | Only that `rollbackOnFailure:true` is passed on a single-feature update, so batch rollback is not exercised here (it is elsewhere — M-02). No two-writer case. | **keep** — the reference mutation pattern for the whole repository |
| **M-02** / P1 | `rollbackOnFailure` across layers; GeoServices REST + store layer; seeded PostGIS; Pro | A failing edit anywhere in the batch rolls the whole transaction back and the prior state is restored. | `tests/dotnet/Honua.Server.Tests/FeatureServerEndpointTests.cs:4362::ApplyEdits_ServiceLevel_DefaultRollbackOnFailure_RollsBackAllLayerEdits` (`count == 0`); `tests/dotnet/Honua.Server.Tests/Features/FeatureStore/PostgresFeatureStoreIntegrationTests.cs:684::ApplyEdits_WithOrderedOperations_PreservesRequestOrderWithinRollbackTransaction` (`WasRolledBack == true`, `"Operation rolled back."`, restored attribute re-read) and `:718::WriterTransaction_LaterLayerFailure_RollsBackEarlierLayer` (Serializable cross-layer) · real PostGIS · api-integration · no | None material. | **keep** |
| **M-03** / P1 | Per-feature edit conflict codes (#2251); GeoServices REST; Pro | `NotFound`, `DeleteNotFound`, `InvalidObjectId`, **`UpdateConflict`** and **`FeatureLocked`** are returned per feature with stable codes. | `FeatureServerApplyEditsConflictCodeTests.cs` — three HTTP tests (`ApplyEdits_UpdateMissingFeature_ReturnsNotFoundCode`, `…DeleteMissingFeature…`, `…DeleteNonNumericObjectId…`) against `EditHarness`, which swaps in the in-memory `TestFeatureStore`; plus six pure `ClassifyWriterFailure_*` unit tests · unit + api-integration on a fake store · no | `UpdateConflict` and `FeatureLocked` are asserted **only** by the classifier unit tests; greps (`PreconditionFailed`, `UpdateConflict`, `423`, `featureLocked`) find no test producing either through HTTP. The three HTTP tests do not read back and run against the fake store, so they prove the codes for `TestFeatureStore`'s behaviour, not Postgres's. | **strengthen** — [server#4406](https://github.com/honua-io/honua-server/issues/4406) |
| **M-04** / P1 | At-most-once `applyEdits` across nodes (#2250/#3052); GeoServices REST; Redis-backed idempotency; Pro | A retry with the same idempotency key repeats the original outcome and **creates no second row**. | `FeatureServerApplyEditsIdempotencyReleaseTests.cs` — 6 tests over real PostGIS with the writer swapped for `ReadOnlyFeatureWriter` / `ThrowingFeatureWriter` / `CancelFirstTransactionalFeatureWriter`, covering retry after provider rejection, after transactional cancellation, after unknown-outcome write, after an ambiguous zero-count result · api-integration · no | Every assertion is `ReadErrorCode(body).Should().Be(...)`; **the database is never queried after the retry**, so these prove "no duplicate 200", not "no duplicate rows". And all three files named `ApplyEditsIdempotencyStoreTests` construct the store with `MemoryDistributedCache` or `cache: null`, so the `SET NX` Lua path at `ApplyEditsIdempotencyStore.cs:338` — the actual multi-node mechanism — is untested. | **strengthen** — [server#4406](https://github.com/honua-io/honua-server/issues/4406) |
| **M-05** / P0 | Two concurrent editors on the same feature; all write protocols; Pro | A defined outcome: no lost update, no torn batch, the documented conflict result. | **Now covered for the success path**, by [#4380](https://github.com/honua-io/honua-server/pull/4380), which added two real concurrent-writer tests to `FeatureServerMutationScenarioTests.cs`: `ApplyEdits_ConcurrentAttributeOnlyUpdates_PreserveGeometryAndReportSuccess` (8 concurrent HTTP `applyEdits` on the same objectId via `Task.WhenAll`, then a read-back asserting the name and both geometry ordinates survive) and `ApplyEdits_ConcurrentDistinctPartialUpdates_RereadAfterLockedSnapshotChanges` (three writers forced into a genuine race by holding `SELECT … FOR UPDATE` and polling `pg_blocking_pids` until all three block, then asserting **all three** field writes survived — a lost-update proof). `CollectionsEndpointsConcurrencyTests.cs` is a unit test of a projection helper. `ODataETagConcurrencyTests` exercises `If-Match` on **GET** only (`GetFeature_WithNonMatchingIfMatch_Returns412PreconditionFailed`); there is no stale-ETag PATCH/PUT/DELETE test. `OgcFeaturesTransactionTests.DeleteFeature_WithStaleIfMatch_Returns412` asserts the 412 and never verifies the feature survived. · — · no | What remains missing is narrower than when this row was first written: **no concurrent-writer test produces a conflict code** — both new tests assert success paths. `ODataETagConcurrencyTests` still exercises `If-Match` on GET only, and the concurrent-overwrite defect [#4252](https://github.com/honua-io/honua-server/issues/4252) is still open. | **strengthen** — [server#4406](https://github.com/honua-io/honua-server/issues/4406), existing [#4252](https://github.com/honua-io/honua-server/issues/4252) |
| **M-06** / P1 | Feature locks and version tokens (`/featureLocks`, `RequireLock` / `RequireVersionToken` / `LockOrVersion`) | A write is refused while another editor holds the lock or presents a stale version token. | 26 passing tests: `FeatureEditGuardTests.cs` (12), `InMemoryFeatureLockServiceTests.cs` (10), `FeatureLockEndpointsTests.cs` (4) · unit + api-integration · no | **`IFeatureEditGuard` has no consumer.** `grep -rn "EditGuard" src tests` returns exactly one non-test line in `src/` — its DI registration at `FeatureLockServices.cs:15` — and `grep "IFeatureEditGuard\|EvaluateAsync" src/Honua.Protocols.GeoServices/FeatureServer/` returns nothing. The endpoints hand out leases nothing honours. The only implementation is node-local (`InMemoryFeatureLockService`). Advertised edit capability **is** now enforced, contrary to this audit's first pass: [#4234](https://github.com/honua-io/honua-server/pull/4234) added the `undeclaredOperation` gate at `FeatureServerEditsHandler.cs:200-213` ("Metadata capabilities are an editing contract, not merely advisory text") with `FeatureServerEndpointTests.cs:3217::ApplyEdits_RejectsEditKindMissingFromDeclaredCapabilities` asserting a 400 and `"Create edits are not enabled"`. Optimistic concurrency is also better covered than first reported: OGC API Features enforces `If-Match`/412 on PUT, PATCH and DELETE (`OgcFeaturesTransactionTests`, BH2-002 regression block at `:469-472`) and `TestFeatureStorePreconditionTests` covers ten store-level precondition-token cases including `ApplyEdits_UpdateWithStaleToken_FailsTypedAndDoesNotWrite`. The gap that survives is specific: **the GeoServices `applyEdits` surface has no `If-Match`/version-token gate**, and the lock guard is dead code. | **strengthen** — [server#4402](https://github.com/honua-io/honua-server/issues/4402) (needs an operator decision on enforce-or-label) |
| **M-07** / P1 | OGC API Features CRUD durability; OGC API; real PostGIS; Pro | Each state is readable through the API after the mutation; a rejection changes nothing. | [`OgcFeaturesMutationScenarioTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Protocols.OgcApi.Tests/Source/Features/OgcFeaturesMutationScenarioTests.cs) — `MutationLifecycle_CreateReplacePatchDelete_RoundTripsEachState` re-reads via `GET …/items/{id}` at every step (`"mutation-created"` → `"mutation-replaced"` → `"mutation-patched"` → 404) and `InvalidMutations_AreRejectedWithoutChangingStoredState` asserts `numberMatched` unchanged and the original value three times · fresh schema per test, real PostGIS, auth bypassed · api-integration · no | None material. This is the OGC twin of M-01. | **keep** |
| **M-08** / P1 | OGC API Features and OData single-entity CRUD as commonly tested; OGC API / OData; real PostGIS; Pro | The mutation is durable and readable. | `OgcFeaturesTransactionTests.cs` — `CreateFeature_WithValidGeoJson_ReturnsCreated` asserts `Properties.Should().ContainKey("name")` (key only); `UpdateFeature_WithValidGeoJson_ReturnsUpdated` asserts the id and that a `name` key exists but **never that the value became `"Updated Feature"`**; `DeleteFeature_WithValidId_ReturnsNoContent` has no GET-after-delete; `PatchFeature_WithPropertiesOnly_ReturnsUpdatedFeatureAndPreservesGeometry` asserts geometry `NotBeNull` rather than the original coordinates. `ODataCrudEndpointTests.cs` — all six create/update tests assert the server's own echo and none re-GETs; `DeleteFeature_WithValidId_ReturnsNoContent` never reads back (the one verified OData delete is `ODataGeometryCrudTests.cs:367`). · api-integration · no | A no-op PUT that echoes the request would pass several of these. Redundant with M-01/M-07, which do it properly. | **strengthen / consolidate** — [server#4406](https://github.com/honua-io/honua-server/issues/4406) |
| **M-09** / P1 | "The mutation persists even when the event publisher fails" | The row is committed regardless of the event-publish failure. | `OgcFeaturesTransactionTests.cs` — `CreateFeature_WhenEventPublishFails_ReturnsCreated`, `DeleteFeature_…`, `UpdateFeature_…`, `PatchFeature_…`; `OgcFeaturesBatchOperationTests.cs::Batch_WhenEventPublishFails_ReturnsSuccess` · api-integration · no | All five assert only the status code or `SuccessCount`. **None reads the row back**, so a mutation that was rolled back and returned 201 anyway would pass all five — which is precisely the failure mode the tests exist to exclude. | **strengthen** — [server#4406](https://github.com/honua-io/honua-server/issues/4406) |
| **M-10** / P1 | Batch / `$batch` atomicity; OGC API + OData; real PostGIS; Pro | A failing member rolls the whole changeset back. | Good: `OgcFeaturesBatchOperationTests.cs` — `Batch_WithPartialFailure_ReturnsMultiStatus` (`424` + `finalCount == initialCount`), `Batch_WithFailFast_StopsAfterFirstError`, `Batch_WithDeleteThenUpdate_RollsBackEarlierDelete` (re-GETs the feature, `200`). `ODataAdvancedFeaturesTests.cs:354::Batch_WithAtomicityGroupFailure_RollsBackChanges` counts rows directly in Postgres. · api-integration · no | `ODataMultipartBatchTests::MultipartBatch_WithChangeset_SupportsAtomicity` sends **one successful POST and no failing operation**, asserting `responseBody.Should().Contain("HTTP/1.1 201")` — a test named for atomicity that never exercises it. `MultipartBatch_WithPostRequest_CreatesFeature` has no read-back. | **keep** the JSON-batch tests; **strengthen** the multipart ones — [server#4406](https://github.com/honua-io/honua-server/issues/4406) |
| **M-11** / P0 | Attachment upload / replace / delete; GeoServices REST + storage; Pro | Metadata and object stay consistent; a byte-exact round trip; an interrupted or rejected upload leaves nothing behind. | Store layer, real Postgres + real `LocalFileStorage`: `PostgresAttachmentStoreTests.UploadAsync_WithValidFile_CreatesAttachmentAndStoresFile` (exact bytes) and `DeleteAsync_ExistingAttachment_DeletesAttachmentAndFile` (row gone **and** object gone) are genuine. API layer: of 31 tests in `AttachmentEndpointTests.cs`, exactly one asserts bytes (`UpdateAttachment_WithReplacementFile_ReplacesStoredContent`). · api-integration · no | `PostgresAttachmentStore` writes object and row with **no shared transaction**; all three compensating `catch` blocks are logged-only and have zero coverage. `CreateAsync_WithMetadata_CreatesAttachmentRecord` deliberately asserts success on a row pointing at a nonexistent object. `DownloadAttachment_WithValidId_ReturnsFileContent` asserts `NotBeEmpty()` against known 17-byte content. `DeleteAttachments_WithValidIds_ReturnsSuccess` never re-queries. Three tests use `AssertGeoServicesErrorAsync(415, 500)`, and the helper skips body inspection for any status ≥ 400 in the list, so a 500 crash passes a media-type test. No attachment test runs off the dev-auth bypass. All 19 tests in `FileUploadSecurityTests.cs` are pure-unit validator calls tagged `[Endpoint(".../addAttachment")]`, so any attribute-derived coverage table will over-credit that endpoint by 19. | **strengthen** — [server#4404](https://github.com/honua-io/honua-server/issues/4404), existing [#4250](https://github.com/honua-io/honua-server/issues/4250) |
| **M-12** / P1 | Replica management admin authorization and replica ownership (Preview surface, floor retained); GeoServices REST | Only an admin lists or reads replicas; one principal cannot drive another's replica. | Ownership **is** implemented and unit-tested, contrary to this audit's first pass: `ReplicaRecord.OwnerId` (`src/Honua.Core.Abstractions/Features/FeatureStore/Domain/ReplicaRecord.cs:30`), persisted by `PostgresReplicaRepository.cs:92/160/194` and enforced at five sites in `FeatureServerRequestHandlers.Replication.cs` (`:203`, `:261`, `:515`, `:1107`, `:2012`) via `IsReplicaOwnerOrAdmin` → `ReplicaSecurity.CanAccess` (`ReplicaSecurity.cs:17-20`), with the cross-principal theory `FeatureQuerySecurityMatrixTests.cs:174::ReplicaOwnershipMatrixEnforcesOwnerPolicy` (owner / other-owner / legacy-null / admin-override rows). Admin authorization: `ReplicaManagementEndpointTests.cs:128::ListReplicas_WithoutAdminAuthorization_IsRejected` and `:203::GetReplica_WithoutAdminAuthorization_IsRejected` — the **only two test methods in the whole mutation and replica surface that disable the dev-auth bypass**, and their anonymous→401 half is real. · unit + api-integration · no | The admin half of both tests is `NotBe(Unauthorized)` + `NotBe(Forbidden)`, satisfied by 400/404/500. In `GetReplica_…` it is **guaranteed vacuous**: the replicaId is `"00000000000000000000000000000000"`, which `GetReplica_ForUnknownReplica_ReturnsNotFound` (`:198`) uses to assert a 404. Neither test ever produces a 403 — no authenticated non-admin is tried, so the admin-only claim is untested in the direction that matters. Ownership is proven only at unit level: **no endpoint test drives principal B's client against principal A's replicaId** through `synchronizeReplica` / `extractChanges` / `unRegisterReplica`. `CreateReplicaRequest.ReturnAttachments` (`ReplicationModels.cs:35`) is still parsed and read nowhere in `src/`, and appears nowhere in `tests/dotnet`. | **strengthen** — [server#4405](https://github.com/honua-io/honua-server/issues/4405) |
| **M-13** / P1 | Offline-sync durability (Preview): conflict recording, manual review, rollback, generation cursors | Conflicts are recorded and applied per policy; a failed upload does not advance the generation; a stale cursor cannot clobber a concurrent sync. | Strong, real PostGIS: `FeatureServerReplicaSyncTests` — `SynchronizeReplica_ConcurrentServerEdit_RecordsConflictAndAppliesLastWriteWins`, `…_ManualReviewConflictHandling_WithholdsConflictingEdit` (`serverName == "server-value"`), `…_RollbackOnFailure_AbortsUploadLeavingStateUnchanged`; `ReplicationDurabilityTests` — `SynchronizeReplica_UploadFailure_DoesNotAdvanceGeneration`, `TryUpdateSyncState_StaleExpectedCursors_DoesNotClobberConcurrentSync` (a real CAS test); `ReplicaConflictReviewEndpointTests` (24 real-DB tests with state read-back) · api-integration · no | "Concurrent" here is sequential ordered awaits. The 53 test members in `ReplicaConflictResolutionServiceTests` (1773 lines, 51 `[UnitTest]`, zero `[IntegrationTest]`) are all over `FakeConflictRepository`; no `*ConflictRepository*` integration test exists, so the claim CAS is unproven against Postgres. `DistributedReplicaStoreTests::SetGetRemove_ConcurrentOperations_…` gives each of its 40 tasks a distinct key, so there is no contention. Two vacuous-pass cases: `ChangeTrackingBaselineDeltaTests::ChangeLogBaseline_CoversSeededFeatures_FromGenerationZero` (early return on empty) and `SynchronizeReplica_UploadThenExtract_ExcludesOwnEdits` (unguarded `foreach`). | **keep** the durability suite; **strengthen** — [server#4405](https://github.com/honua-io/honua-server/issues/4405) |
| **M-14** / P0 | Durable job queue: at-most-once claim, lease fencing, worker-crash recovery | Exactly one worker claims a job; a reconciler never fails or requeues a job a live worker owns; a crashed job is recovered without a duplicate side effect or a lost result. | Strong and real: `JobExecutionServiceTests`' ownership/fencing family (`ProcessJob_SkipsExecution_WhenClaimOwnerChangedBeforeRunningTransition`, `HeartbeatPump_Stops_WhenOwnershipChanges`, `PublishArtifact_Skips_WhenOwnershipLost`, `FinalizeJob_SuccessRacingDurableCancellation_FinalizesAsCancelled`); `RedisExecutionSubstrateIntegrationTests::ExecutionJobStore_WithRedis_FinalizesOnlyWhileLeaseIsOwned` and `…_RequeuesHeartbeatExpiredJobForRetry` (real Redis, real re-claim) · unit + api-integration (real Redis) · no | Three defects. **(a)** `JobReconciliationService` calls only `TrySetAsync` (lines 222/265/341/417), yet `JobReconciliationServiceTests` asserts `DidNotReceive().SetAsync(...)` at lines 97, 138, 180, 232, 274, 322, 432, 478 — assertions on a method the code cannot reach. Two tests have **no other assertion at all**: `HeartbeatExpiry_SkipsTransition_WhenClaimChangedToAnotherWorker` (:147) and `…_WhenJobDeleted` (:249). The first is exactly the don't-steal-a-live-claim test, and it would pass if the reconciler failed every job it swept. **(b)** The Lua claim script's `return 0` branch (`RedisJobQueue.cs:45-51`) is unreached: every `ScriptEvaluateAsync` stub in the 16 tests of `RedisJobQueueTests.cs` returns `"1"` (lines 130, 176, 283, 350, 426, 486, 572; the only non-`"1"` continuations are `Task.FromException` at `:77` and `:131`), and no test races two workers. **(c)** No crash test observes a counted side effect: the real `kill -9` test is `[RequiredEnvironmentFact("HONUA_WORKER_IMAGE")]`, hand-writes the stale heartbeat, and runs `MaxAttempts=1`; `TileExportDurableRecoveryRedisTests::WorkerLoss_…` has a "lost worker" that never executed anything; `RedisJobExecutionResilienceTests` restarts the broker, not the worker, and tolerates re-execution by design. There is no dead-letter concept in the job system to test. A recovery invariant rides on a `[FlakyTest]` (`RedisExecutionSubstrateIntegrationTests.cs:715`, test at `:716`, #812). | **strengthen** — [server#4403](https://github.com/honua-io/honua-server/issues/4403), existing [#3851](https://github.com/honua-io/honua-server/issues/3851) |
| **M-15** / P1 | Read-after-write freshness through the response cache | A committed edit is visible to the next read within the contract's freshness bound. | None. Greps for `read-after-write`, `ReadAfterWrite`, `readYourWrite` return zero hits repo-wide. `QueryResultCacheManagerTests` has two tests and no invalidation case; `MvtTileCacheTtlTests` asserts only `Cache-Control` max-age. The nearest is a metadata-cache regression, `AttachmentEndpointTests::AddAttachment_ThenGetAttachmentInfos_ReturnsNewAttachmentNotStaleCache`. · — · no | The defect is filed ([#4259](https://github.com/honua-io/honua-server/issues/4259), warm replicas serving stale feature responses for 30 s); this row is the missing test asset behind it. | **strengthen** — existing [#4259](https://github.com/honua-io/honua-server/issues/4259) |
| **M-16** / P1 | Transactional outbox: no silent CDC loss on the edit path | An edit and its outbox row commit atomically; a rolled-back edit leaves no outbox row. | The strongest crash-atomicity test in the repository is `tests/dotnet/Honua.Server.Tests/Features/Alerts/AlertEvaluationAtomicityTests.cs::CommitEvaluationAsync_FailureAtEveryBoundary_ReplayCommitsOneTransition` — real Postgres, isolated schema, a throw injected at **every** `AlertEvaluationCommitBoundary`, asserting nothing partially committed, replay idempotent, exactly one transition. `PostgresAlertOutboxWriterTests::AppendAndEnqueueAsync_PersistsEventAndDispatchTogether` is also real Postgres. · api-integration · no | Both are **alerts**, not feature edits or jobs. On the edit path the outbox tests are `[UnitTest]` over fakes (`OutboxDispatcherBackgroundServiceTests`) and the Postgres-side test is wiring-only (`FeatureDataAccessOutboxScopeWiringTests`). No real-Postgres test asserts that an `applyEdits` and its outbox row commit in one transaction. `ExecuteOnceAsync_MultipleNodes_OnlyOneClaimsRow` is two sequential awaits whose exactly-one outcome is guaranteed by the fake's queue — the test's own comment concedes it emulates `FOR UPDATE SKIP LOCKED` rather than exercising it. | **strengthen** — [server#4403](https://github.com/honua-io/honua-server/issues/4403) |
| **M-17** / P1 | SDK query and mutation against the candidate; JS / Python / .NET | The SDK sends the right credential, receives the exact server result, and preserves denial semantics. | Mocked in bulk: JS stubs transport in 111 of 548 `*.test.ts` via injected `fetchFn` / `vi.stubGlobal("fetch", …)`; Python uses `httpx.MockTransport` in 42 of 98 `test_*.py`; .NET uses `MockHttpHandler : HttpMessageHandler` in ~152 test files. Real-server lanes exist in all three: JS `test/integration/surfaces/*.integration.ts` against `ghcr.io/honua-io/honua-server@sha256:…`; Python `tests/conformance/` (digest-pinned **and** requiring an installed wheel — `pytest.fail("HONUA_SDK_INSTALL_ROOT is required; certification must use an installed wheel")`); .NET `Conformance.Tests` (digest-pinned, `gh attestation verify`, and the **only required real-server check in the fleet**) · api-integration (out-of-process) · digest-pinned but pre-cut, so no candidate receipt | **No SDK test in any language asserts a 401 or 403 from a real server.** Greps across all three repos' integration/conformance suites and `scripts/ci/run-sdk-server-compatibility.sh` return nothing. Every real-server lane is plain HTTP — `sdk-server-compatibility.yml:214` sets `Authentication__PortalToken__RequireHttps: 'false'` with the comment *"The Actions service network is HTTP-only"* — so TLS, a GA deployment promise, is unproven for the SDKs. JS `test/integration/harness.ts` still degrades to `describe.skip` when `HONUA_INTEGRATION_BASE_URL` is unset, so an unconfigured local run exercises zero surfaces — though the CI lane no longer relies on that path: `integration.yml` now spins the digest-pinned `ghcr.io/honua-io/honua-server@sha256:78e3088d…` container in-workflow and reports executed suite and test counts, stating that a push or schedule run never reports a silent green skip. It still has **no `pull_request` trigger**, so JS PRs land on mock evidence only. Python's conformance lane is the strongest in the fleet and is **not required**; its staging lane defaults to the mutable tag `ghcr.io/honua-io/honua-server:latest-aot`. `tests/esri-client-compat/` and `tests/python/esri_client_compat/` are referenced by no workflow, script or pytest testpath. | **strengthen** — see release#7 (installed-client execution) and the #3805 TLS + canonical-fixture overlay |
| **M-18** / P1 | MCP as a GA access path; MCP; Pro | MCP applies the same per-layer/per-service resource policy as REST, and a denied principal receives no records. | `ServicePublishTransportAuthorizationTests::PublishAuthority_RealTransportTwentyFourCaseMatrix_MatchesRest` (real HTTP, `HONUA_DEV_AUTH=false`, OIDC bearer, 24 cases, `AssertZeroActuationAsync()`) and `Studio/StudioMcpOwnershipParityIntegrationTests::RestAndMcp_ApplyTheSameOwnerAndCrossUserDenialRules` are both genuine parity proofs · api-integration · no | Both cover **mutations** (`service.publish`, Studio drafts), never reads. The per-layer gate landed with [#4346](https://github.com/honua-io/honua-server/pull/4346) and is covered at unit level by `McpMapToolTests` (`QueryFeatures_DeniedResourcePolicy_DoesNotReadFeatures`, `DescribeLayer_DeniedPolicy_DoesNotExposeMetadataOrReadCount`, `ListLayers_DeniedPolicy_OmitsRestrictedLayerAndCount`) against a substituted `IFeatureReader` — not against a fixture holding real rows, and not compared with the REST result. `CrossProtocolPermissionMatrixTests` has no MCP row, and no MCP test uses `ServiceRbacTestFixture`. `MapToolSchemas.MaxLayerLimit = 500` / `MaxFeatureLimit = 1000` appear only in `src/`, so the data-result caps are untested (discovery bounds *are* well covered — `McpPaginationTests.DefaultSurfaceLimits_AreBounded`, `McpDiscoveryToolTests.ListCapabilities_DefaultIsBoundedAndAdminFullExportIsExplicit`). The MCP certification lane has never run: `ci.yml:122` pins `MCP_SDK_REF` to a commit that lacks `test:certification`, the lane skips clean, and CI Gate treats a skip as a pass — the workflow comment says so, and the ledger records the surface as `bounded-child-ticket` (#484). | **strengthen** — [server#4389](https://github.com/honua-io/honua-server/issues/4389), existing [#4255](https://github.com/honua-io/honua-server/issues/4255), [#4251](https://github.com/honua-io/honua-server/issues/4251) |
| **M-19** / P1 | Edition gates on licensed operations; Community / Pro / Enterprise | An operation below its `MinimumEdition` returns the documented payment result; at or above it executes. | [`tests/dotnet/Honua.Server.Tests/Features/Licensing/EntitlementSweep/`](https://github.com/honua-io/honua-server/tree/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Server.Tests/Features/Licensing/EntitlementSweep) — `EntitlementProbeRegistry` derives its list from `FeatureCatalog.All.Where(f => f.MinimumEdition > HonuaEdition.Community)` (44 probes, 17 documented no-HTTP-surface allowlist entries, an **empty** `KnownGaps`); `EntitlementSweepCoverageTests.EveryGatedKey_HasExactlyOneRegistryRow` fails the build when a new gated key with an HTTP surface has no row; `EntitlementSweepHttpTests` asserts 402 below and non-402 at or above, per edition · api-integration · no | This is a genuinely fail-closed asset and the best-designed suite in the audit. It proves **gate wiring**, not license lifecycle: `LicenseTestSupport.WithTestLicense` swaps in an in-memory `TestLicenseEntitlementService`, so signature verification, file loading and expiry never run behind an HTTP request. | **keep** |
| **M-20** / P1 | License failure-mode contract (operator amendment, 2026-09-05, option B STRICT) | Pro/Enterprise refuse to start on a missing, invalid or expired license; expiry stops licensed operations and cancels in-flight jobs with reason `"license expired"` leaving no partial output marked complete; reads and exports are licensed; renewal takes effect on restart; warnings at 30/14/7/1 days. | **Implemented and tested**, by PR [#4358](https://github.com/honua-io/honua-server/pull/4358) (`bc83e84187`). Startup: `FileBackedLicenseServiceTests` now has `StartAsync_MissingLicenseFile_RefusesStartup`, `_MalformedLicenseFile_RefusesStartup`, `_OversizedLicenseFile_RefusesStartup`, `_UnknownSigningKey_RefusesStartup`, `_InvalidSignature_RefusesStartup`, `_ExpiredLicense_RefusesStartup` (the six tests that previously asserted a fail-soft Community downgrade). Runtime: [`LicenseRuntimeContractTests`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Server.Tests/Features/Licensing/LicenseRuntimeContractTests.cs) — `WarningSchedule_EmitsThirtyFourteenSevenOneDayThresholdsOnce`, `Expiry_InFlightJob_CancelsAndFailsWithoutCompletedPartialOutputs` (theory over `ignoreCancellation`), `Renewal_ChangedSource_RevalidatesOnIntervalAndSurvivesRestart`; and [`LicenseFailureContractTests.Transitions.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Server.Tests/Features/Licensing/LicenseFailureContractTests.Transitions.cs) — `RuntimeExpiry_RegisteredWorker_CancelsAndFailsPublishedPartialOutput`, `Renewal_RegisteredService_ReloadsChangedFileAtOneMinuteAndOnRestart`. Source: the cancellation reason `"license expired"` plus an incomplete-output marker is emitted by `ExecutionJobReconciler.cs:249`, `ExportJobService.cs:282`, `UniversalImportJobService.cs:352` and both import background services; the graduated schedule is `FileBackedLicenseService.Runtime.cs:136` (`foreach (var days in new[] { 30, 14, 7, 1 })`); reads and exports are gated by the new `LicenseOperationMiddleware` and `ILicenseOperationPolicy`. · unit + api-integration · no | No candidate-bound receipt, and the transitions are exercised in-process rather than against a started server image, so the documented startup exit code is proven at the service layer rather than at the process boundary. `docs/concepts/editions-and-licensing.md` was rewritten in the same PR and should be re-read against the site licensing page. | **keep** — [server#4399](https://github.com/honua-io/honua-server/issues/4399) was filed against the stale checkout and is closed as not planned |

---

## Priority 3 (continued) — Geoprocessing: the whole `BuiltInProcessCatalog`

The 2026-09-04 reaffirmation puts the **whole** catalog inside the supported denominator with no per-operation Preview carve-out, and requires each row to carry "a real input-to-decoded-output execution receipt, not a flag assertion against a fake runner". This section states the denominator honestly and records the one structural fact that governs the whole family.

**The denominator is 98 operations**, not 17: `src/Honua.Geoprocessing/Features/Geoprocessing/BuiltInProcessCatalog.cs` declares 98 distinct `ProcessId` values — analytics 9, conversion 6, data-management 4, enrichment 1, gdal 2, generalization 2, geometry 14, imagery 1, import 1, overlay 6, pcloud 1, proximity 4, raster 12, sink 4, source 8, statistics 3, surface 8, transform 12.

| ID / sev | Contract | Expected behaviour | Proof (fixture · layer · receipt) | Gaps | Disposition |
|---|---|---|---|---|---|
| **GP-01** / P1 | 12 managed geometry operations — `geometry.area/buffer/centroid/clip/convex-hull/dissolve/intersect/length/project/simplify/snap/union` | Submitted through the public API, executed by the durable runtime, and the decoded result matches a golden expectation. | [`VectorProcessParityIntegrationTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Server.Tests/Features/Geoprocessing/Execution/VectorProcessParityIntegrationTests.cs) (`[Collection("Redis")]`, real durable runtime, real production executor) and `GeometryBufferJobExecutorIntegrationTests.cs::BufferProcess_CompletesAndReturnsBufferedGeometry` — each submits to `POST /ogc/processes/processes/{id}/execution`, polls to a terminal status, retrieves `GET /ogc/processes/jobs/{jobId}/results`, and compares the published GeoJSON Feature / FeatureCollection / measure-result against a golden expectation (geometry type, feature count, area or coordinate position, measure value) · api-integration · no | `geometry.difference` and `geometry.make-valid` are not covered by this harness. | **keep** — this is the receipt shape the whole catalog needs |
| **GP-02** / P1 | `transform.reproject` | The output coordinates reflect a real datum shift. | `GdalWorkerExecutorTests.cs:635::VectorReproject_WithRealOgr2Ogr_AppliesNad27ToWgs84DatumShift` — real `ogr2ogr`, EPSG:4267 → 4326 near Meades Ranch, parses the output GeoJSON and asserts `(deltaLon + deltaLat) > 1e-6` ("not an identity transform") **and** each `delta < 0.01` ("sub-kilometre, not a CRS mistake") · unit (real tool) · no | Not run on the required gate (GP-04). | **keep** — the **only** decoded-output receipt against a real tool in the entire geoprocessing surface, and the correct oracle template |
| **GP-03** / P0 | The 17 operations the operator named as partially proven — `surface.slope/aspect/hillshade/roughness/rugosity-tri/rugosity-tpi/contour/viewshed`, `proximity.euclidean-distance/-allocation`, `raster.clip/mosaic/reproject/resample/zonal-statistics`, `imagery.classify`, `pcloud.translate` | A real input-to-decoded-output execution receipt per operation. | **Five of the seventeen now have one.** [#4378](https://github.com/honua-io/honua-server/pull/4378) added [`RasterExecutionProofTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Worker.Gdal.Tests/RasterExecutionProofTests.cs) — production executors driven through `DockerGdalCommandRunner` against the pinned production GDAL image read out of `docker/worker-gdal/Dockerfile`, with analytical cell oracles. It covers `raster.clip`, `raster.mosaic`, `raster.reproject`, `raster.resample`, `raster.zonal-statistics` (five of the 17) plus `raster.histogram`, `raster.interpolate-idw` and `raster.spectral-index`. Examples: `Reproject_GeographicToMercator_MatchesAnalyticalGridAndInverseMappedSamples` (theory over nearest/bilinear), `Mosaic_OverlappingTwoBandTiles_ProvesPrecedenceAndNoDataFallback` (first/last policy), `ZonalStatistics_MultipleReadWindows_MergesExactCountsSumsAndPopulationVariance` (`variance` and `stddev` to 1e-9). For the remaining twelve the best available evidence is one real-tool test with a magic-byte oracle (`GdalSurfaceExecutorTests.cs:302::Slope_WithRealGdaldem_ProducesGeoTiff_AndReconcilesAgainstSource`) and argument assertions against `FakeGdalCommandRunner`. · unit (real tool, in the production image) · no | For `surface.*`, `proximity.*`, `imagery.classify` and `pcloud.translate` the position is unchanged: `FakeGdalCommandRunner.Succeeding` writes the **test's own bytes** to the output path (`FakeGdalCommandRunner.cs:40-46`), so every "decoded artifact" assertion against it is circular. Two tests are easy to misfile as oracles: `ZonalStatistics_OneZone_PublishesScalarWithAggregatesPerZone` parses 25/3/1/5/75 out of `ZonalGdalinfoFixture`, a hardcoded `gdalinfo` stdout the test authors — it proves the parser; and `ImageryInferenceJobExecutorTests::ExecuteAsync_RasterOutput_PublishesGeoTiffArtifactPreservingBackendBytes` asserts the test's own fixture came back from a stub handler. `gdal_euclidean_allocation.py` — a script Honua ships — is still never executed. `Slope_…_AndReconcilesAgainstSource` still overstates its evidence in its own name. | **strengthen** — the per-operation family `#3913`-`#3951` plus [server#4400](https://github.com/honua-io/honua-server/issues/4400); five rows are now dischargeable against `RasterExecutionProofTests` |
| **GP-04** / P1 | Real-tool geoprocessing coverage in CI | The real-GDAL/PDAL cases execute and cannot silently disappear. | **Real GDAL now runs on the required per-PR gate.** `pr-gate.yml:228-240` ("Prove raster catalog execution with production GDAL", [#4378](https://github.com/honua-io/honua-server/pull/4378)) pulls the image named in `docker/worker-gdal/Dockerfile` and runs `Honua.Worker.Gdal.Tests --filter 'Category=RasterExecutionProof'` with a 10-minute timeout, uploading the TRX. Separately, `ci.yml`'s `worker-gdal-tests` job (`:1332`, gated on `integration_changes` at `:1351`) installs `gdal-bin` (`:1384`), asserts the binaries exist (`:1387`), sets `HONUA_REQUIRE_GDAL_CLI=true` (`:1365`) and — the right mechanism — re-runs `--filter "Category=GDAL"` into its own TRX and **fails on outcome counts** (`:1416-1470`, `::error::` guards at `:1464` and `:1468`: any skipped case, or fewer than one passed, is an error). · unit (real tool) · no | The three `[GdalCliFact]` tests remain off the required path: `lean-gate/action.yml:238` filters `Tier=Fast` and `GdalCliFactDiscoverer` (`GdalCli.cs:197-199`) emits `Tier=Integration`; `ci.yml` has no `pull_request` trigger. **The new PR-gate raster step has no counts tripwire of its own** — a filter that matched nothing would exit 0 silently. **PDAL has never been executed by any test** (`grep PdalCliFact\|HONUA_REQUIRE_PDAL` → nothing repo-wide; no workflow installs it; the only invocation is `pdal --version` at `ProductionWorkerContainerHandoffTests.cs:201`). The container test is `[RequiredEnvironmentFact("HONUA_WORKER_IMAGE")]` (`:36-39`) with no counts tripwire, so a dropped variable turns that lane green with zero cases. | **strengthen** — [server#4401](https://github.com/honua-io/honua-server/issues/4401) |
| **GP-05** / P0 | Public-API execution of a GDAL/PDAL catalog operation | Submit → real worker → decoded output, read back through the public API. | **Zero operations have this.** The only full loop is `scripts/qualification/gp-lifecycle-harness.sh`, which submits `gdal.ogr2ogr` against a digest-pinned production worker and reads the results document — but it is a shell script rather than a test, its workflows are `workflow_dispatch`-only requiring three hand-supplied `image@sha256` digests, and its verification is a `sha256sum` non-emptiness check (`:206`), so a numerically wrong output passes. · — · no | The gap is **structural**. `grep -rln 'Honua.Worker.Gdal' --include=*.csproj tests/` returns three projects, and neither `Honua.Server.Tests` nor `Honua.Protocols.OgcApi.Tests` — the only ones with a `WebAppFixture` and the execution routes — is among them; `Honua.Worker.Gdal.Tests` has zero `HttpClient`/`WebApplicationFactory` references. Closing this needs a project reference or a new test project, which changes the effort estimate on all 39 per-operation issues. Meanwhile the public-API execution tests that do exist accept failure as a pass (`Execute_RasterSurfaceProcess_IsNotRejectedAsUnknownProcess` accepts `Created`, `ServiceUnavailable` **or** `InternalServerError`), and `OgcProcessesStagedArtifactContentTests::ArtifactContent_SucceededJob_StreamsStagedObject` stamps `ProducingEngine = RasterOutputContract.GdalWorkerEngine` (`:339`, `:431`) on 32 KB of synthetic non-GeoTIFF bytes and serves them as `image/tiff`. [#4363](https://github.com/honua-io/honua-server/pull/4363) later made that payload deterministic (`CreatePayload` at `:535`) rather than `Random.Shared.NextBytes` and added a genuinely stronger sibling, `ArtifactContent_RestoredAttestedVolume_PreservesDescriptorAndOracleChecksum` (`:66`), which pins a SHA-256 across a volume backup and restore — real, but still not GDAL provenance. | **strengthen** — [server#4400](https://github.com/honua-io/honua-server/issues/4400), [server#4401](https://github.com/honua-io/honua-server/issues/4401) |
| **GP-06** / P1 | Catalog truthfulness | Every advertised catalog row is executable. | `ProcessExecutionCapabilityCatalog.cs` partitions the 98 ids and carries one `UnavailableReasons` entry — `raster.interpolate-kriging`, *"No kriging-capable numerical backend is bundled in this build"* — surfaced by `BuiltInProcessCatalog.cs:22-25` as `AdvertisedButNotExecutableProcesses`. · — · no | A per-operation carve-out exists in source against a ruling of no per-operation carve-outs. This needs an operator decision, not a test. | **strengthen** — existing [#3932](https://github.com/honua-io/honua-server/issues/3932); operator decision required |

---

## Good patterns to preserve

Strengthening work below means "look like these". None of them may be weakened or deleted.

1. **Authorization with a zero-side-effect assertion** — `tests/dotnet/Honua.Server.Tests/Import/ImportAuthorizationParityTests.cs::ImportAuthority_RealTransportTenCaseMatrix_MatchesRestAdminWriteContract`. Two surfaces (REST and MCP) × five principal profiles over real HTTP; every denial asserts `_effects.Should().BeEmpty(...)` and `DidNotReceive().ImportFileAsync(...)` alongside the status. This is the shape every denial row in this map is asking for.
2. **Per-operation RBAC as a cross-protocol matrix** — `CrossProtocolPermissionMatrixTests.cs`. A theory over 7 edit-kind combinations × grant-present/absent × FeatureServer and gRPC, plus OData, OGC API Features and OGC API Tiles, with both the grant-honoured and the fall-back-to-coarse-deny direction asserted.
3. **Mutate, read back, then prove the invalid mutation changed nothing** — `FeatureServerMutationScenarioTests` and `OgcFeaturesMutationScenarioTests`, in both halves.
4. **Filtering asserted by exact ids** — `OgcFeaturesEnhancementsTests::GetItems_WithDateTimeParameter_FiltersFeatures` (`ids.Should().Equal(1, 2, 4)`), `ODataFilterMatrixTests`, `ODataBboxShorthandTests` (`Contain`/`NotContain` by named id).
5. **A decoded-output oracle against a real tool** — `VectorReproject_WithRealOgr2Ogr_AppliesNad27ToWgs84DatumShift`, whose two-sided bound catches both an identity-transform bug and a wrong-CRS bug.
6. **Byte-exact decode against an independent implementation** — `TileDecompressorFixtureTests` (10 GDAL-produced COG fixtures, byte-compared against GDAL's own decode, no skip conditions).
7. **Fail-closed coverage derived from the product** — `EntitlementProbeRegistry` builds its probe list from `FeatureCatalog` itself, so a new gated key with an HTTP surface and no probe fails the build, and `KnownGaps` is empty.
8. **Fault injection at every commit boundary** — `AlertEvaluationAtomicityTests::CommitEvaluationAsync_FailureAtEveryBoundary_ReplayCommitsOneTransition`: a throw at each boundary, nothing partially committed, replay idempotent, exactly one transition.
9. **Counts-based anti-skip tripwire in CI** — `ci.yml:1478-1535` fails when any `[GdalCliFact]` case is skipped or when none passed. This is how "skipped required cells are non-passing evidence" is enforced in practice, and it should be copied to the container and PDAL lanes.
10. **Public-API submit → poll → decode** — `VectorProcessParityIntegrationTests` and `GeometryBufferJobExecutorIntegrationTests`. The harness exists and works; it simply has not been pointed at the GDAL/PDAL half of the catalog.

## Weak-assertion patterns found, as a checklist

Recorded so part 2 and the burn-down can grep for them rather than rediscover them.

- Accepting refusal alongside success — `BeOneOf(OK, Unauthorized)`, `BeOneOf(OK, Accepted, Unauthorized)`.
- Accepting a server error as a pass — `BeOneOf(Created, ServiceUnavailable, InternalServerError)`.
- `NotBe(Forbidden)` / `NotBe(Unauthorized)` / `NotBe(NotFound)` in place of an exact status.
- Shape-only — "the response has a `features` array", `NotBeEmpty()`, magic bytes (`PAR1`, PNG signature, `II`/`MM`).
- Denial asserted by status alone, with no state or disclosure check.
- Echo-only mutation assertions with no read-back.
- Filtering asserted by a per-row re-check of the same predicate, proving no false positives but never no false negatives.
- Vacuous pass — a `foreach` with no non-empty precondition, or an early `return` on an empty fixture.
- Assertions on a method the production code cannot call (`DidNotReceive().SetAsync` where only `TrySetAsync` exists).
- A test name that promises behaviour the body never exercises (`…SupportsAtomicity` with no failing member; `…RecoversAcrossRestart` with no restart; `…ConcurrentOperations…` with disjoint keys; `…ReconcilesAgainstSource` with a magic-byte check).
- Sequential awaits standing in for concurrency.
- Fixtures that make the assertion circular — a fake runner that writes the test's own bytes; a fake store that returns hardcoded content when nothing was written; a hand-authored tool stdout parsed back as if measured.
- Silent skips — `pytest.skip` on a 404/501, `Skip` when a binary is absent, `describe.skip` when an env var is unset, `[RequiredEnvironmentFact]` with no counts tripwire.
- `[Endpoint]`/`[Operation]` annotations on tests that never issue a request (all 15 in `FileUploadSecurityTests.cs`).

## Coverage limits

This is a manual review of representative areas. It is not a claim that every test was audited, and the disposition column says which rows were read and which were not.

**Method and its limits.** Every row above was produced by reading test bodies, fixtures and the code under test. Where a row says "none found", the greps that were run are named in the linked issue. Three method limits are worth stating plainly:

- Coverage was assessed by reading, not by executing. No test was run and no coverage tool was used, so a test that is currently failing, quarantined or excluded by a trait filter may be cited above as if it passes. `[FlakyTest]` and `[RequiredEnvironmentFact]` tags are called out where found, but not exhaustively.
- `[Endpoint]` and `[Operation]` annotations were treated as untrustworthy, on evidence: all 15 tests in `FileUploadSecurityTests.cs` tag `POST …/addAttachment` while calling only static validators. Any coverage table derived from those attributes will over-credit. The API-surface inventory guard is an inventory, not proof.
- No candidate-bound receipt exists for any row, because no pre-cut candidate exists. Every "no" in the receipt column is the same "no".

**Named areas inside the packet's scope that were NOT reviewed:**

- Priority 1: the full Enterprise edition matrix beyond the entitlement sweep's registry; identity paths (OIDC, SAML, SCIM, mTLS client certificates) beyond their edition gates; admin API-key and session lifecycle; row-level security and field-mask policy endpoints beyond their denial assertions; the Console and Studio surfaces; gRPC beyond its rows in `CrossProtocolPermissionMatrixTests`.
- Priority 2: the import and tile-operations job systems beyond the recovery comparison in M-14; the alerting evaluation and dispatch path (Preview) beyond `AlertEvaluationAtomicityTests`, which is cited as a pattern; realtime feature-stream subscriptions (Preview); the OGC Processes `dismiss` and job-ownership suites beyond noting they run under the dev-auth bypass; SensorThings; STAC and OGC API Records, which are GA as the catalog surface and are deferred to part 2.
- Priority 3: ImageServer and its WMTS, OGC API EDR, OGC API Coverages, NAServer and VersionManagementServer (all Preview) beyond the Zarr and COG rows that pass through them; FlatGeobuf, GeoPackage, Shapefile, CSV, GPX and FileGDB import/export fidelity, which has its own hunt family (#4271-#4278); 3D, I3S, scenes and point clouds (Experimental) apart from `pcloud.translate`; warehouse providers (Experimental); the 81 catalog operations outside the 17 named plus the 12 managed geometry ones were surveyed for test-file association only, not body-audited, so no evidence class is asserted for them.
- Cross-cutting: install, upgrade, rollback, backup and restore; real-ECS and the two-revision upgrade gate; load, soak and capacity; the release train, lock and evidence-ledger machinery; `honua-console`, `honua-studio`, `honua-iac`, `honua-helm` and `honua-devops`; the SDK repositories beyond the transport, required-check and authorization survey in M-17; the geospatial-MCP specification repository.

**Deferred to part 2** (per the operator's ruling c sequencing): every GA GP operation row by row; install, migration, upgrade and restore; and the remaining read-only contract behaviour, including STAC, OGC API Records, and the discovery and metadata surfaces.

**On re-verification against trunk.** The first pass of this audit read a local `honua-server` checkout that was 36 hours and 108 commits behind trunk. Every row and every filed issue was then re-verified against `origin/trunk` at `ac5723545a`. Nine rows changed materially:

| Row | Change |
|---|---|
| A-11 | Resolved — the aggregate quality-score test was removed by [#4357](https://github.com/honua-io/honua-server/pull/4357). Issue #4391 closed as not planned. |
| M-20 | Resolved — the strict license failure-mode contract was implemented and tested by [#4358](https://github.com/honua-io/honua-server/pull/4358). Disposition **strengthen → keep**. Issue #4399 closed as not planned. |
| GP-03, GP-04 | [#4378](https://github.com/honua-io/honua-server/pull/4378) added `RasterExecutionProofTests` — production executors against the pinned production GDAL image with analytical cell oracles, covering five of the seventeen named operations plus three more, and it runs **on the required PR gate**. The claim that no catalog operation had a decoded-output receipt, and that real-GDAL coverage could not block a merge, is no longer true. |
| M-05 | [#4380](https://github.com/honua-io/honua-server/pull/4380) added two genuine concurrent-writer `applyEdits` tests, one of which forces a real race with `SELECT … FOR UPDATE` and `pg_blocking_pids` and proves no lost update. The remaining gap is that neither produces a conflict code. |
| M-06 | [#4234](https://github.com/honua-io/honua-server/pull/4234) made advertised edit capability enforceable and added a test for it. The dead `IFeatureEditGuard` half of the row stands. |
| A-08, M-18 | [#4346](https://github.com/honua-io/honua-server/pull/4346) closed [#4255](https://github.com/honua-io/honua-server/issues/4255) and added MCP denial tests that assert the reader is never called. The REST-vs-MCP parity gap stands. |
| A-04, A-05, A-06 | [#4192](https://github.com/honua-io/honua-server/pull/4192) added the first authorization assertion to `Honua.Protocols.OgcClassic.Tests` (WFS `GetFeature`), and [#4239](https://github.com/honua-io/honua-server/pull/4239) inverted the WMS denial from HTTP 200 to 401. |
| M-12 | **An error in the first pass, not drift.** The claim that replicas have no owner was wrong when written: `ReplicaRecord.OwnerId` existed at the stale revision too, enforced at five handler sites and covered by a unit theory. The audit grep was scoped to the Esri wire DTO and missed the domain record. The surviving gap is the absence of an HTTP-level cross-principal test. |

Counts and line numbers throughout were re-checked and corrected; several were wrong on the day rather than stale, and are marked as such in the issues.

**On the two rows this review corrected.** An earlier draft of this map recorded `Wfs20EndpointsTests.Wfs_Transaction_AnonymousWrite_AllowsInsertWithoutRbac` as "a direct false security proof". It is not: the test sets `AccessPolicy { AllowAnonymousWrite = true }` on the resource before writing, so it exercises an opt-in policy correctly. Its name is misleading and is dispositioned for a rename in [server#4387](https://github.com/honua-io/honua-server/issues/4387), not for removal. The same draft treated the dev-auth bypass as universal; it is the default but 134 sites opt out, and two of the surfaces that looked unproven — OGC API Features and OData write authentication — are among them. Both corrections came from reading the fixtures rather than the names, which is the whole point of this exercise.

---

# 2026.1 GA evidence map — part 2

Owner: [release#210](https://github.com/honua-io/honua-release/issues/210) · audit run 2026-09-05 · scope: priorities 4–6 (every GA GP operation in the `BuiltInProcessCatalog`; installation, migration, upgrade, rollback and restore including ECS and Lambda; the remaining read-only and API-contract behaviour). Rows continue part 1's series and use part 1's schema, execution layers and disposition vocabulary unchanged; facts **F1** (the dev-auth bypass default), **F2** (the authorization fixture has no database) and **F3** (the required per-PR gate) apply here exactly as stated above and are not repeated.

> **Audited revision — part 2.** Part 2 was read at `honua-server` `acefb812cf`, one commit ahead of the document's pinned revision `ac5723545ac008b1f0357b35d0cbb8bfd67eff39`. That single commit is [#4367](https://github.com/honua-io/honua-server/pull/4367) (atomic style-preset promotion) and it touches exactly three files — `src/Honua.Core/Features/Styling/Abstractions/IStyleCatalog.cs`, `src/Honua.Db/Postgres/Features/Styling/PostgresStyleCatalog.cs`, `tests/dotnet/Honua.Ai.Tests/Source/McpStylePresetIntegrationTests.cs` — none of which part 2 cites. **Every file cited below is byte-identical at both revisions**, so the whole document stays pinned to one revision and every part-2 line number was verified there. Other repositories read for part 2, each at its own default branch head: `honua-release` `34653c0f19`, `honua-iac` `0136dea179`, `honua-devops` `669ab5e61a`, `honua-console` `c582ea556a`, `honua-studio` `2b323aea28` (default branch `main`), `honua-migrate` `bc763c51c9`. SDK revisions are unchanged from part 1. Every file in part 2 was read through `git show <ref>:<path>`; no working tree was consulted.

**Contract deltas applied in part 2.** The 2026-09-06 amendment ([release#282](https://github.com/honua-io/honua-release/issues/282)) promotes **AWS Lambda from Preview to a GA target**, carrying the full ECS bill — live deploy, serving smoke, upgrade, rollback, destroy — on the real serverless substrate. The 2026-09-05 ruling b (release#210) makes the **two-revision ECS application upgrade and rollback** a §14 cloud gate that runs *after* the candidate cut. Both are reflected in priority 5 below.

## Two facts that bound priority 4

**(F4) The catalog is not one denominator, it is four.** `src/Honua.Geoprocessing/Features/Geoprocessing/ProcessExecutionCapabilityCatalog.cs` partitions the 98 catalog rows into `Job` (**79**), `ProtocolOnly` (**6** — `analytics.cluster`, `analytics.density`, `data-management.copy-features`, `data-management.delete-features`, `data-management.calculate-field`, `conversion.geometry-format`), `WorkflowOnly` (**12** — the eight `source.*` and four `sink.*` rows) and `Unavailable` (**1** — `raster.interpolate-kriging`). The canonical reasons are in source: ProtocolOnly is *"Callable only through its owning synchronous protocol endpoint; no process-job executor is registered"*; WorkflowOnly is *"Composable only as a workflow DAG source or sink; direct process execution is not supported."* `ProcessExecutionCapabilityCatalog.Classify` throws when a row has anything other than exactly one classification, and `IsOgcCallable` narrows the OGC API Processes projection to the job-callable set. So **19 of the 98 GA catalog rows cannot be submitted to `POST /ogc/processes/processes/{id}/execution` at all** — a carve-out nineteen times larger than the single kriging row part 1's GP-06 recorded.

**(F5) A committed per-operation evidence matrix already exists, and it is enforced on the required gate.** `certification/gp-operation-matrix.v1.json` carries one verdict per catalog id, and `tests/dotnet/Honua.Architecture.Tests/GeoprocessingOperationEvidenceMatrixTests.cs` — `[Trait("Category","Architecture")]`, so it runs in **both** required gate steps (`Honua.Architecture.Tests` at `lean-gate/action.yml:254` and `Honua.Server.Tests --filter "Category=Architecture"` at `:296`) — fails the build when the manifest's id set drifts from `BuiltInProcessCatalog.ListProcesses()`, when a non-`proven` row lacks a concrete follow-up issue URL, when two rows share a gap issue, when an `unproven` row claims evidence, or when a cited evidence file no longer contains the cited test name. Its `statusDefinitions` and its `insufficientEvidence` list — *catalog registration, discovery or description, dispatcher routing, HTTP status alone, success flag alone, nonempty output, file signature or magic bytes, mocked algorithm output* — are the same standard this audit applies. **Part 1 did not cite this asset**; its GP section reported the 81 unnamed operations as "surveyed for test-file association only". They were not: they carry committed verdicts, and this audit verified a sample of them against the test bodies.

Current manifest state at `acefb812cf`: **67 proven · 27 partially-proven · 4 unproven**, `catalogVersion` `honua.process_catalog.builtin.v1`, `audit.recordedAt` 2026-09-02, `audit.catalogSourceRevision` `14d101ec17`.

## Priority 4 — Every GA GP operation in the `BuiltInProcessCatalog`

| ID / sev | Contract | Expected behaviour | Proof (fixture · layer · receipt) | Gaps | Disposition |
|---|---|---|---|---|---|
| **GP-07** / P1 | The whole-catalog execution-evidence denominator itself; all 98 rows; GA | Every catalog row carries an explicit, auditable evidence verdict, and a catalog addition cannot land without one. | [`certification/gp-operation-matrix.v1.json`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/certification/gp-operation-matrix.v1.json) + [`GeoprocessingOperationEvidenceMatrixTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Architecture.Tests/GeoprocessingOperationEvidenceMatrixTests.cs) — `ManifestProcessIds_ExactlyMatchBuiltInCatalog` (`BeEquivalentTo(catalogIds)`), `ManifestRows_HaveAuditableEvidenceOrOneConcreteGapIssue` (issue URL regex `^https://github\.com/honua-io/honua-server/issues/\d+$`, `OnlyHaveUniqueItems` across gap issues), `ManifestSummaryAndSharedRuntimeReferences_MatchRows` · **required PR gate** · no | Three weaknesses, none fatal. **(1)** The evidence check is `File.ReadAllText(evidencePath).Should().Contain(testName)` — a substring match on the method *name*. Deleting every assertion in a cited test, or gutting its body to `Assert.True(true)`, leaves the matrix green; the `assertion` field is free prose the guard never compares against anything. **(2)** `audit.catalogSourceRevision` is `14d101ec17` (recorded 2026-09-02) and nothing enforces it: the guard pins `catalogVersion`, a constant string, so an algorithm change inside an executor cannot invalidate a `proven` verdict. Three commits have touched `src/Honua.Geoprocessing/.../Execution/` since that revision. **(3)** `sharedRuntimeGaps` is asserted with `Should().Equal(Enumerable.Range(3848, 10)…)` — a hardcoded literal range, so closing one of #3848–#3857 requires editing the test rather than the manifest. | **keep** — the best structural asset in the geoprocessing surface and the pattern the rest of the release should copy; **strengthen** per the three items — [server#4408](https://github.com/honua-io/honua-server/issues/4408) |
| **GP-08** / P0 | Catalog truthfulness under the 2026-09-04 no-carve-out reaffirmation; OGC API Processes + GPServer + WPS; GA; any authorized caller | Every advertised catalog row is executable, or the product says plainly where it is not. | The classification is real and fail-closed, and it is proven: [`CatalogExecutableConformanceTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Server.Tests/Features/Geoprocessing/Execution/CatalogExecutableConformanceTests.cs) — `Catalog_ClassifiesEveryProcessExactlyOnce` (98 / 79 / 6 / 12 / 1), `EveryManagedJobProcess_HasADispatcherExecutor`, `EveryManagedDispatcherExecutor_HasAJobOrWorkflowClassification`, `ProtocolUnavailableAndNativeProcesses_AreAbsentFromTheManagedDispatcher`, `GeometryFamily_IsFullyJobExecutable_AndMatchesTheSyncExecutionPolicy`, `NativeProcesses_DeclareAnExplicitCapabilityAndStayOffTheManagedDispatcher`; plus [`GeoprocessingCatalogDocParityTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Architecture.Tests/GeoprocessingCatalogDocParityTests.cs) — `EveryCatalogProcessId_IsDocumentedInReference` and `DocProseCount_MatchesCatalogCount`, which pins the prose count in `docs/reference/geoprocessing-operations.md` against the catalog · `[UnitTest]` + `[ArchitectureTest]` · **required PR gate** · no | The tests prove the classification is *coherent*, not that it is *permitted*. **F4**: 19 GA catalog rows are not job-callable. That is a per-operation carve-out in source against a ruling of no per-operation carve-outs, and it is nineteen rows rather than the one part 1's GP-06 recorded. It is not a defect on its face — a `sink.*` row is a DAG node by design — but the release's public claim ("the whole `BuiltInProcessCatalog` is GA") and the OGC Processes projection do not currently say the same thing, and the six `ProtocolOnly` rows are advertised in a *process* catalog while being callable only from FeatureServer/analytics endpoints. **This needs an operator ruling, not a test.** | **strengthen** — existing [#3932](https://github.com/honua-io/honua-server/issues/3932); **operator ruling required** — [server#4409](https://github.com/honua-io/honua-server/issues/4409) |
| **GP-09** / P1 | The 40 managed vector operations — `overlay.*` (6), `statistics.*` (3), `transform.*` (12), `proximity.near`/`near-table`, `generalization.*` (2), `analytics.*-managed` (5), `analytics.buffer-aggregate`, `analytics.spatial-join`, `enrichment.enrich`, `data-management.append`, `conversion.feature-project`, plus the 14 `geometry.*` rows; GA; authorized caller | The production executor consumes concrete input and emits decoded output matching a hand-derived oracle. | [`ManagedExecutorTestHarness.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Server.Tests/Features/Geoprocessing/Execution/ManagedExecutorTestHarness.cs) builds inline `data:application/geo+json;base64,…` FeatureCollections, runs the **real** executor through the canonical step-parameter envelope, and decodes the published artifact with the production `GeoJsonArtifactCodec`. On it: `OverlayExecutorTests` (`Clip_TruncatesInputGeometryToClipUnion` — `Geometry.Area.Should().BeApproximately(50, 1e-6, "the eastern half (x:5..10) survives the clip")`; `Union_EmitsInputOnlyOverlayOnlyAndIntersectionPieces` — `HaveCount(3)` + summed area `150`; `Erase_FullyCoveredFeatureIsDropped` — `BeEmpty()`), `StatisticsExecutorTests` (`Calculate_ComputesDescriptiveStatisticsPerField` asserts `COUNT`, `MIN`, `MAX`, `MEAN = 10/3`, `SUM`, and `STDDEV = 1.1547005383792515` to `1e-9` with the derivation in a comment; `Summarize_NullCaseValueFormsOwnGroup_AndNullsSkippedInAggregate`), `RelationalTransformExecutorTests` (11), `SpatialTransformExecutorTests` (7), `SourceExecutorTests` (6), `ProximityExecutorTests` (5), `LayerSourcedExecutorTests` (11), `AttributeTransformExecutorTests` (12), `StreamingTransformExecutorTests` (11), `EnrichmentJobExecutorTests` (24), `Managed{Cluster,Density,HotSpot,SpatialJoin,BufferAggregate}ExecutorTests` (34). **Every one is `[UnitTest]` → `Tier=Fast` → on the required PR gate.** · unit, production executor, real NetTopologySuite · no | Unit layer by construction: no HTTP, no durable runtime, no Postgres, no persistence read-back. Input is an inline data URI rather than a stored layer, so schema inference, large inputs, streaming spill and artifact staging are outside these tests. `conversion.feature-project` is the one row in this family the matrix still calls partially-proven, because `LayerSourcedExecutorTests::FeatureProject_RequestsServerSideReprojection_ReachesSucceeded` runs against a fake source that returns unprojected coordinates. | **keep** — the largest body of honest decoded-output evidence in the product, and it is on the required gate; this is what part 1's GP section could not see |
| **GP-10** / P1 | The `ProtocolOnly` rows with real-database evidence — `analytics.cluster`, `analytics.density`, `data-management.delete-features`; GeoServices REST; seeded PostGIS; Pro | The synchronous protocol endpoint computes the documented result over real rows. | `SpatialAnalyticsRestTests.QueryDensity_SquareGrid_ReturnsExactCellCountForSeededPoints` — real PostGIS via `WebAppFixture`, and the test derives its oracle in a 10-line comment (five seeded features, one with NULL geometry excluded, the remaining four projected to EPSG:3857 and binned into a 20 km `ST_SquareGrid` at cells `(-682,225) (-683,226) (-679,224) (-681,227)`), then asserts `features.GetArrayLength().Should().Be(4)` and `featureCount == 1` on every cell; `QueryClusters_Dbscan_PerFeature_ReturnsClusterAssignments`; `FeatureServerEndpointTests::DeleteFeatures_WithWhereClauseInBody_DeletesMatchingFeatures` (exact deleted id + follow-up query proving zero matching rows) · `WebAppFixture`, real PostGIS, auth bypassed (F1) · api-integration · no | Not on the required gate (F3). `QueryDensity_…` is an explicit regression for [#2945](https://github.com/honua-io/honua-server/issues/2945), where the previous density tests asserted only `featureCount > 0` — the fix is recorded in the test's own comment and is the model for the rest of the analytics surface. The remaining three `ProtocolOnly` rows (`data-management.copy-features`, `data-management.calculate-field`, `conversion.geometry-format`) are the weak half: the matrix records `copy-features` and `geometry-format` as **unproven**, and `calculate-field`'s only evidence, `FeatureServerMaintenanceTests.cs:182::Calculate_ValidRequest_ReturnsSuccess`, is weaker than the manifest concedes: it writes `name = 'Updated'` across the layer and then asserts `root.GetProperty("success").GetBoolean().Should().BeTrue()` and `root.TryGetProperty("updatedFeatureCount", out _).Should().BeTrue()` — the **existence of a key**, not its value, and no row is ever re-read. A `calculate` that updated nothing, or updated every row to the wrong value, passes. | **keep** the density/cluster/delete rows — reference pattern; **strengthen** — existing [#3937](https://github.com/honua-io/honua-server/issues/3937), [#3940](https://github.com/honua-io/honua-server/issues/3940), [#3936](https://github.com/honua-io/honua-server/issues/3936) |
| **GP-11** / P0 | The 31 catalog rows with no decoded-output receipt at any layer — 27 partially-proven + 4 unproven; GA (2026-09-04 reaffirmation) | A real input-to-decoded-output execution receipt per operation. | The manifest states each gap in its own words and binds it to exactly one issue. The dominant pattern is `FakeGdalCommandRunner`: `surface.aspect/hillshade/rugosity-tri/rugosity-tpi/roughness` are *"Fake-runner command projection only"*, `surface.contour`/`surface.viewshed`/`conversion.polygonize`/`conversion.rasterize`/`conversion.raster-format`/`conversion.raster-reproject`/`raster.map-algebra`/`raster.reclassify`/`gdal.gdalwarp` are *"Fake-runner … and artifact shape only"*, and `raster.statistics` *"Parses synthetic CLI stdout; no real raster computation."* `surface.slope` is the one that runs real GDAL and still falls short: `GdalSurfaceExecutorTests.cs:305::Slope_WithRealGdaldem_ProducesGeoTiff_AndReconcilesAgainstSource` asserts `artifact.Should().StartWith("data:image/tiff")` and the `II`/`MM` byte pair, and nothing else — the name's "reconciles against source" is not exercised anywhere in the body. `pcloud.translate` validates a fake PDAL pipeline. `imagery.classify` preserves *simulated backend bytes*. The four unproven rows are `raster.interpolate-kriging` (no bundled backend), `conversion.geometry-format`, `data-management.copy-features`, `source.postgis`. · unit against fakes · no | Unchanged from part 1's GP-03 in substance, now stated across the whole catalog rather than the 17 named rows: **of the 17 must-pass operations the 2026-09-04 amendment names, five (`raster.clip/mosaic/reproject/resample/zonal-statistics`) were discharged by [#4378](https://github.com/honua-io/honua-server/pull/4378) and twelve remain** — the eight `surface.*`, both `proximity.euclidean-*`, `imagery.classify` and `pcloud.translate`. `RasterExecutionProofTests` is the proven template and the fixture corpus (`tests/dotnet/Honua.Worker.Gdal.Tests/Fixtures/RasterProof/`, with `generate.py`, `decode.py` and a README stating the per-operation oracle) is already in place; extending it to the twelve is mechanical work, not new design. | **strengthen** — the per-operation family [#3913](https://github.com/honua-io/honua-server/issues/3913)–[#3951](https://github.com/honua-io/honua-server/issues/3951) (one issue per row, enforced unique by GP-07's guard), plus [server#4400](https://github.com/honua-io/honua-server/issues/4400) and [server#4401](https://github.com/honua-io/honua-server/issues/4401) |
| **GP-12** / P1 | GP execution through the public API with the durable runtime and a decoded artifact; OGC API Processes; real PostGIS + Redis; Pro; restricted submitter | Submit → durable job → published artifact, read back through the results document, with the submitter's data policy applied. | **This shape does exist, contrary to part 1's GP-05 framing — for managed sources.** [`GeoprocessingJobRowAndFieldSecurityTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Server.Tests/Features/Geoprocessing/GeoprocessingJobRowAndFieldSecurityTests.cs) — `LayerSourcedJob_ForRlsAndFieldMaskRestrictedSubmitter_ArtifactIsRowFilteredAndMasked` sets `builder.UseSetting("HONUA_DEV_AUTH", "false")` (`:164`), submits a `source.honua-layer` plan over HTTP, polls to `successful`, `GET`s the results document, base64-decodes the published FeatureCollection artifact and asserts the **exact restricted row count** and that the masked attribute is absent — against a baseline run that asserts the full count and the attribute present. `LayerSourcedJob_PinsSubmitterSecurityContextOnDurableRecord` asserts the durable record carries the submitter's claims. `VectorProcessParityIntegrationTests` (`[Collection("Redis")]`, `[IntegrationTest]`, 11 cases) and `GeometryBufferJobExecutorIntegrationTests` do the same loop for the geometry family with golden expectations. · real auth, real PostGIS, real Redis, public API · api-integration · no | The loop is proven for **managed** executors only. Nothing routes it through the GDAL/PDAL worker — part 1's GP-05 project-boundary finding stands unchanged (`Honua.Worker.Gdal.Tests` has no `WebApplicationFactory`; `Honua.Server.Tests` does not reference `Honua.Worker.Gdal`). None of these run on the required gate. The RLS/field-mask test is the strongest end-to-end GP asset in the repository and should be named as the template on [#4401](https://github.com/honua-io/honua-server/issues/4401) rather than a new harness being designed. | **keep**; **strengthen** — existing [server#4401](https://github.com/honua-io/honua-server/issues/4401) |
| **GP-13** / P1 | GP job authorization: process-execute grant, per-layer read gate, mutating tier, job ownership, dismiss; OGC API Processes; Pro; execute-only principal / cross-owner principal | A baseline execute grant does not become a data-read grant or a mutation grant; one caller cannot read, list, retrieve results for, or cancel another's job. | Genuinely strong, and these are among the few protocol tests that turn the bypass off. [`OgcProcessesLayerAccessAuthorizationTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Protocols.OgcApi.Tests/Source/Processes/OgcProcessesLayerAccessAuthorizationTests.cs) — two published layers, one with a role-restricted `AccessPolicy`, a submitter holding only `process:*:execute`; `Execute_LayerSourcedPlanWithoutLayerReadGrant_ReturnsForbidden`, `Execute_SpatialJoinWithUnauthorizedJoinLayer_ReturnsForbidden`, `Execute_HonuaLayerSourceWithoutLayerReadGrant_ReturnsForbidden`, and `Execute_UnresolvableLayerId_ReturnsForbiddenWithoutRevealingExistence` which additionally asserts `body.Should().NotContain("not found")`. `OgcProcessesMutatingTierAuthorizationTests` proves the mutating-tier gate (#2798) the same way. [`OgcProcessesJobOwnershipTests.cs`](https://github.com/honua-io/honua-server/blob/ac5723545ac008b1f0357b35d0cbb8bfd67eff39/tests/dotnet/Honua.Protocols.OgcApi.Tests/Source/Processes/OgcProcessesJobOwnershipTests.cs) — `HONUA_DEV_AUTH=false` at `:141`; `JobList_MixedOwnership_ReturnsOnlyCallerJobs` asserts `jobIds.Should().Equal("owned-job")`, `JobResults_CrossOwnerFailedJob_ReturnsNotFoundWithoutErrorLeak` asserts the body `NotContain("private failure detail")`, and `DismissJob_CrossOwnerJob_ReturnsNotFound_AndDoesNotCancel` re-reads the job and asserts it is still `Running` · api-integration · no | The positive controls in both authorization files are `NotBe(HttpStatusCode.Forbidden)` rather than an exact status — the "not 403" pattern on part 1's checklist. It is defensible here (they exist to show the denial is a *layer* decision, not a blanket rejection) but it means a positive control would survive the plan failing for an unrelated reason. `OgcProcessesJobOwnershipTests` runs against an in-memory job store (F2-shaped), so the ownership filter is proven at the handler, not against durable rows. | **keep** — `DismissJob_CrossOwnerJob_ReturnsNotFound_AndDoesNotCancel` is the exemplar denial: status, no leak, **and** no side effect; **strengthen** the positive controls to exact statuses |
| **GP-14** / P2 | Test-tier hygiene on the required gate (this row is about the gate, not a product operation) | A test that proves a GA behaviour is reachable by the required gate's filter. | `lean-gate/action.yml:238` selects `--filter "Tier=Fast"`, and `Tier` is set only by a TestKit attribute (`UnitTestAttribute` → `Category=Unit` + `Tier=Fast`). `TestAttributeEnforcementTests.cs` enforces `[Protocol]`/`[Operation]`/`[Endpoint]` on `[IntegrationTest]` classes and **nothing about tiers**. · — · no | **1732 test methods across 195 files in `Honua.Server.Tests` are bare `[Fact]`** and carry no `Tier` trait, so `--filter "Tier=Fast"` excludes every one of them from the required gate; a new test written with a plain `[Fact]` silently lands off it. `ExternalPostgisSinkExecutorTests` — cited by the GP matrix as `proven` evidence for `sink.external-postgis` with a real PostGIS read-back — is one of them. They do run in the trailing full matrix, so this is a gate-reachability observation under the adopted eventual-quality model, not a coverage hole. The annotations that *are* enforced are the ones part 1 showed to be untrustworthy. | **strengthen** — [server#4410](https://github.com/honua-io/honua-server/issues/4410) |
